# 派聪明库表设计

> 来源：派聪明库表设计.pdf

> 解析时间：2026-03-14 16:23:59

> 本文档由 OCR 识别生成，可能存在少量识别错误


---


## 文档信息


- **总页数**: 5

- **文件大小**: 6.23 MB


---


## 第 1 页


14 文件上传解析设计方案

15 知识库检索设计方案

16 聊天助手设计方案

17 派聪明库表设计

1 接口文档

星球

 派陪明 RAG 项目的库表设计
一、用户表 (users)

users 表不仅存储了基础的用户认证信息(用户各、加密密码) ，更重要的是实现了基于组织的权限管理体系。通过 role 字
段区分普通用户和管理员， org_tags 字段支持用户归属多个组织标签 (过号分陋) ，而 primary_org 则标识用户的主要组织
归属。这种设计使得派聪明能够在企业环境中实现细粒度的多租户权限控制，确保不同组织的用户只能访问属于自己组织的
知识资源-

字段名         数据类型                  是否主角

训                         是

Usemame 。。 VARCHAR255)            理           用户名, 唯一

pasaword 。。 VARCHAR255)            理           加密后的密码

role            ENUMCusER: ADMIN) 理           用户角色

omg tags 。。。 VARCHAR255)         理        用户所属组织标签，多个用到号分陋
primany_org 。 VARCHARG5O)             香           用户主组织标和

aeated TIMESTAMP               理           创建时间

pdated at 。 TIMESTAMP               香           更新时间

建表语句:

aear Thate users (
54 BiGIWT Auro_INcREhEMT PRyamy KEY camehr “用户唯一座
aernaee VARGWR(255) NOT NULL UIQUE ComWENT “用户名,只-
pasavord VARGWAR(255) NOT NULL COWENT ,加歼后的灾码…
rele EURCUSER ，'aDMTI) NOT MULL DEFAULT "USER coh “用户角色
erg_tags VARCMAR(255) DEFAULT UL coWENT “用户所属组织标签，多个用旭号分隔"，
primary_org VARCHAR(58) DEFAULT MULL COWEMT “用户主组织标答
created_at TINESTANP DEFAULT CURRENT_TINESTAP COWENT ,创
pdared-t TINESTANP DEFAULT CURRENTTINESTAP ON UPDATE CUPRENL TESTWP CoMT ,更新时间…，
TB idx_username (usernage) CoENT “用户名过可

) GIVE-Tnneb8 DEFAULT CHARSET-utfBnbt CoWENT- 用户来

、组织标签表organization_tags

organization_tags 表支持层级化的组织标签结构。通过 parent_tag 父标签|D，可以构建树形的组织架构 (如: 公司 -> 部
门-> 小组)。

每个标签都有创建者追踪 (created_by ) ，确保组织管理的可追洲性。

在实际业务中，这些组织标签会与文档权限、用户访问控制紧密结合，实现基于组织的知识隔离和共享第路。比如，技术部
门的文档只有技术部门的用户可以访问，而公司级别的公告则所有用户都能看到。

字段名        数据类型          是否主键 描述

tmg间         VARCHARG5O 。 是          标签唯一标识
ame         VARCHAR(IO0 。 理          标签名称
desciption 。 TEXT              和           描述
parenttag 。 VARCHARG5O) 。 理           父标符D
aeated by 。 BlGINT            理           创建者D
areated at 。 TIMESTAMP 。 杰           创建时间
updated at TIMESTAMP 。 否           更新时间

建表语句:

nearE TaatE organization tags (
gd WARCHAR(58) PRIMARY KEY CowwENT 标答叭一球识，
ame VARCHAR(199) NOT NULL COWENT "未签名称…
escrtprion TEXT COWRENT “入送'
Parent tag VARGMAR(58) DEFAULLT MLL Cowewr ，父标签TD，，
created_by BIGINT NOT MLL CoWENT ,创建者10-，
created_at TIMESTAhp DEFAULT CURRENL_TINESTA CoWENT ,创建时|
pdared at TINESTANp DEFAULT CURRENT TINESTAP ON UPDATE CuRREMNL TIESTWP COWEMT ,更新时间…，
FORETGI KEY (parent_rag) REFERENCES Sngandzation tagsttag_id) OW DELETE SET MULL
FOREIGI KEY (creared_by) REFERENCES userstid)

) BysImE-Inne08 DEFAULT CHMARsETrutfBsbt COIENT-"组织标答表

三、文件主表 (file_upload)

fle_upload 表记录了每个文件的完整生命周期

file_md5 实现文件去重，避免重复存储相同内容的文档。 status 字段追踪文件的处理状态 上传中、已完成、处理失
败等) ， org tag 字段将文件与组织权限绪定，is_public 控制文件的可见性范围。 uk_md5_user 唯一约束确保同一用户不
会重复上传相同文件，而 idx_user、idx_org_tag 索引则保证了按用户、按组织查询文件时的高效性。这种设计支持了派陪




## 第 2 页


明的智能文档管理功能，包括文件去重、权限控制和快速检索-

字段各。 类型                                       描述
d            BaNT                                     主键

file_md5 。。 CHARG2) PRIMARY KEY                           文件指纹

le name 。 VARCHAR25)                              文件名
totalsize 。 BlGINT UNSIGNED                                 文件大小 (字区
tahus        TYINTOD                                  文件杖术
userid VARCHARGJ                               用户标识

aeated 二 。 DATETIME DEFAULT CURRENT_TIMESTAMP 。 文件创建时间

merged at DATETME                                  文件合并完威时间

pearE TaatE fine upload (

nmr         MOT Not Auro_IWREMENT ComievT ,主多，
aachsn(a2) NOT NULL ComERT ,文件 NO5…，
agehwR(255) 。 NOT NULL comwENT ,文件名称"

total_stze BIGTMT         MOT NULL CowEWT ,六件大小

status TI        MOT NULL pEFAULT 8 COMENT “上堵区杰，

ser-ia vaRCHAR(6e) IOT NULL comENT -用户 ID，

org_tag 。。 VARCHAR(56) 。。 DEFAULT MLL CONWENT .组织标签"

4s_publtc 。 BOoLEAN        MOT WULL pEFAuLT FALsE comENT ,旦理公开

erged_at 。 TIhESTap WUL DEFAULT MULL on UPDATE CoRRENT_TIMESThwp ComehT ,合并时间，，

PRINARY KEY 《id)，
nrQuE KEY wk_md5_user (file_md5，user_ia)，
Dex ax_user (user_ia)，

Dex lx_org_tag (org_tag)
) ENGIWE-Tmno08 DEFAULT CHARSET-utfgnb4 CORENT- “文件上传记林

四、分片表 (chunk info)

chunk_info 表实现了派聪明的大文件分块上传和存储机制。

当用户上传大型文档时，系统会将文件切分成多个小块，每个分块都有独立的 MD5 校验和存情路径。这种设计不仅提高了
大文件上传的可靠性 (支持断点续传) ，还优化了存储和传输效率。在文档处理流程中，系统可以并行处理不同的文件分
块，加速文档解析和向量化过程。同时，分块存储也为后续的文档版本管理和增量更新提供了技术基础。

nearE maate chemanfe (
le_md5 CHAR(32) ROT NULL COWENT “外刍…，
umic_lndex INT NOT MULL CoWENT

kerage_path VARCMMR(255) NOT NULL CORENT “anTo耻径，
omSTRAINT fk_fle_nd5 FORETGN KEY (fle_md5) REFERENCES 和le_uploadtflls md5)，
DB ldx_ftle_chunk (file nd5，chunk_tndex) -- 联合

) ENGINE-Tmne08

五、解析结果表 (document_vectors)

本块 (chunk id )，每个:

字段名           类型                        撒述

veadorid      BiGINTAUTO_NCREMENT 。 主委，自增D

ie md5        CHARG2)                 文件指纹，用于关联fle upload表
hankaid         NT                         文本分块序号

text_content 。 LONGTECT                   原始文本内容

model version 。 VARCHARG2                生成向量所使用的模型版本

CrEarE Taate document_vecrers (
le_nd5 CHAR(G32) NOT ROLL
chunk id THT NOT MULL CoMENT ,文本分的库号…，
ext_content LONGTBKT COWENT 于纺入
adel_version VARGMR(32) DEFAULT "all -Matt6-v2:
) ETwErImneoy

这五个表构成了派聪明完整的业务闭环: 用户通过 users 表进行身份认证和权限管理，在 organization_tags 定义的组织架
构下上传文档到 fle_upload 表，大文件通过 chunk info 表进行分块管理，最终文档内容在 document_vectors 表中向量化
存储，为智能检索提供数据基础。




## 第 3 页


一一            10人已点时              一

@有SOeG@OSx

国   讨论应以学习和精进为目的。请各发布不友善或者负能量的内容，与人为善，比联明更重要|

息                                                                                                                   0512        评论
热门评论 区
国 muanme                                                        assonazeB teos

5                                          026年0有26日 1635
用户主标签( prinary ora )在系统中起到核心权限错点”的作用，具体功能如下:
工 权限控制的味认依据
。 当用户访问资源 (如文格、知识库) 时，系统优先以 prinazy_cr 作为其轩认"权限标识-

。 例和: 技术部用户的prinary_cr 为 +sch ，则默认只能访问技术部标签下的资源，即使该用户也属于其他
sc)

多场景的简化管理"…




## 第 4 页


， 用户可能属于多个(通过 orq_+ag* 字段存储多个标签， 如 tech ,hr )，但 Primary_orq 明确其主要由属

。 系统在权限校验、数据卫离等场录中，优先使用 pzinazy_cza 作为判断依据，避免复杂的多"交叉权限到各。

3文档与的绑定……
。 用户上传文件时，系统自动接文伯的“zy_*a9 字段设置为用户的primary_cry (除非手动指定)。
确保用户上传的文档款认归属其主，实现*…*级知识隔离如技术部文档对让仅技术部可见)

4 权限策略的快速过下

， 在向量检案 (如 document _vecrors 表) 时，系统通过 “cq :aq 与用户的prinazy_or9 匹配,快速过小无
权访问的文档-

。 提升检索效率，间免用户看到非主"的内容。

“5 必级结构的遇遇

:racea 的侍需在于 -aanizacicn css。 表的 :3 :4 字朋中， 确保用户的主是系统定义的有效季
点。

， 支持层级 (如 company - dept - team )，用户的主可喘射到任意层级节点。

总结

einary_czy 是多租户权限体系的核心错点，通过明耽用户的主要归属，简化权限管理、实现数据内离、优化检索
效率，同时支持灵活的多扩展能力-

1 用四

5条评论

后，                                                            assoamooa oa

引用原文: document vectors 表存储了文档经过 Al模型处理后的向量化表示，每个文档会被切分成多个文本块(
chunk id ) 每个文本块者有对应的向量表示。 text_content 保存原始文本内容，便于检索结果的展示和上下文理解
model version 字段支持向量模型的版本管理，当 AI模型升级时可以识别震要重新向量化的文档。通过 user id 、

org tag 和is_public 字段，向量检索也浊循相同的权限控制策路，确保用户只能检索到有权限访问的文档内容。

 @源下明 向量铺存在哪个字段下?
而吉革国旧81

人                                                        026和3有09日 2033
祖气提供的参考资料，document vectors 表中没有明确说明向量数据存储在时个具体字段下-
参考资料中仅描述了该表的功能和部分字段:

键，自增ID

file_md5: 关联文件表的指扩

chunk id: 文本分块序号

text content” 存铺原始文本内容 【非量)

model version* 生成向量的AI模型版本

虽然表名 -on*_vecccrs 阶示其核心功能是存储向量，但参考资料未明确列出存储向量数据的字自名称。实际
业务中，向量数据通常存储在类似 ve---> 、 *=edain5 或特定命名的字段中(如 vec:ar_sats )，但参考资料

中未提供这一关键信息。
结论:
参考资料未明确说明向量存储的具体字段名称。若需准确答案，需查看完整的表结构定义 〈如建表"中的字段列表)
或补充相关文档-
的
侠w                                                            assoaooa zol

引用原文: document vectors 表存储了文档经过 AI 模型处理后的向量化表示，每个文档会被切分成多个文本块(
chunk jd )，每个文本块都有对应的向量表示。 text_content 保存原始文本内容，便于检索结果的展示和上下文理解-
model version 字段支持向量模型的版本管理，当 Al模型升级时可以识别震要重新向量化的文档。通过 user id 、
org tag 和 is_public 字段，向量检索也进入相同的权限控制策略，确保用户只能检索到有权限访问的文档内容-

向量健存在哪?
而点革 国晶

已的                                   026和03朋06 1957
引用原文: 四、分片表 chunk info)
@浙玫明 是文件出分完片就存储到数据库中吗

而点半”国旧1

]                                                                                     oz6年有06B 1957

根据参考资料中的 chunk info 表设计，分片信息是在文件分片完成后立即存储到数据库的。具体到辑如
相





## 第 5 页


工分片表的作用
hani_info 表专门用于存储文件分片的元数据(如分片泰引、分片MD5、存储路径等) 每个分块对应一条独|

立记录。

了存储时机
当系统桂文件妇分成多个分块{chunk) 后，会立即为每个分块生成元数据，并写入chunk_ints 表。 例如-

。 分片完成后，系统记录 :ils_sd5 (关联文件) 、 -hunk_inaex 《分片序号)、 chunk_nd5 《分片校验
从 、 srcrage_parh (存储位置) 等字段
3 与上传流程的关联
， 分片操作是文件上传流程的一部分 (参考 :iis_uplcaa 表的 “casus 字段这路上传拓态)。
分片完万后，元数据直接写入数据库，确保后续处理 (如合并、向量化) 能通过 #: ls_=35 关联到分片信
息。
为什么需要立即存储?
 。 断吉续传支持: 若上传中断，可过过数据库中的分片记录恢复进度
并行处理: 分片信息存储后，系统可并行处理不同分块 (如向量化)
， 数据一致性: 滥免分片完成后因异常导致元数据天失<

结论:
是的，文件分片完成后，分片元数据会立即写入“huax ins 表，为后续的文件合并、向量化处理提供基础

国

国 muame                                                        xcsoazsa ieat
 @派聪明 用户主"标签页有什么作用
击点共 轩目E1
下 am                                                                       assoazsa teas

用户主标签【primary ora )在系统中起到核心权限错点”的作用，具体功能如下:

工 权限控制的味认依据
。 当用户访问资源 (如文格、知识库) 时，系统优先以 prinary_cry 作为其轩认"权限标识-
。 例各: 技术部用户的 prinary_cry 为 :sch ，则默认只能访问技术部标签下的资源，即使该用户也属于其他

加 se)。

2多场景的简化管理
， 用户可能属于多个(通过 “rd_ ta。字段存储多个标签， 如tech ,he )，但 prisazy_orq 明史其主要月属

， 系统在权限校验、数据阳离等场景中，优先使用 scinazy_cza 作为判断依据，尼免复杂的多"交叉权限远名

3文档与的彤定…
。 用户上传文件时，系统自动文件的“zy_*a9 字段设置为用户的 Frinazy_cry (除非手动指定)。

。 确保用户上传的文档轩认归属其主，实现*……*级知识限离 如技术部文档对认仅技术部可见)

4 权限策略的快速过汉
。 在向量检索 (如 document _vecrors 表) 时，系统通过 “cq :aq 与用户的 Primary_orq 匹配,快速过小无

访问的文档
。 提升检索效率，避免用户看到非主"的内容。

“5 层级结构的遇身
。 primary_orq 的值需存在于 “roanizacion_*ao8 表的 “ao_i9 字段中，确保用户的主是系统定义的有效节

点
。 支持层级 (如 -mpany -dept - team )，用户的主可耽身到任意属级节点。

总结
einazy_cza 是多租户权限体系的核心错点，通过明聊用户的主要归属，简化权限管理、实现数据内离、优化检索

效率，同时支持灵活的多扩展能力-
而 国卓

年12月12日2347

@ -=
为什么tagid不设置为bgint
出3 国上四



