# 知识库检索面试题预测

> 来源：知识库检索面试题预测.pdf

> 解析时间：2026-03-14 16:29:51

> 本文档由 OCR 识别生成，可能存在少量识别错误


---


## 文档信息


- **总页数**: 17

- **文件大小**: 23.01 MB


---


## 第 1 页


20 架构设计面试题预测 显

21 用户管理面试题预测 天
文件上传解析面试是预测 屎
| 23 知识库检索面试题预测“全天
4 联天助手面试题预测 瑟
5 27家真实面经参考       旺球

》 进阶篇

》 Go有本

》 补充入

 派聪明 RAG 知识库检索面试题预测，覆盖 ElasticSearch 的 KNN 和
BM25

1当一个用户在搜索框里输入一句话然后点击搜索， 系统大致会经历一个怎样的处
流程?

首先，用户通过前端页面输入搜索内容并提交，前端会将查询语句、用户信息等参数封装成 HTTP 请求发送到后端。后册接
收到请求后，会解析出查询关键词和用户身份。

在进入搜索去辑前，系统首先会调用外部的 Embedding 模型将用户的自然语言查询转化为向量表示。这一步是实现语义相
似度搜索的基础。同时，系统还会提取出用户对应的组织标签，用于后续的权限过波。

随后，系统会构造出一个 Elasticsearch 混合查询。融合了三类能力: 首先是基于查询向量的 KNN 语义检索，用于找出语义
上虽接近的文本块; 其次是基于关键词的 BM25 检索，用于匹配关键词相似的文档;最后是权限过滤机制，确保返回的文
档必须是公开的、或属于该用户本人，或其组织标等在用户的有效标签列表中

为了提高结果的相关性和精度，我们还会使用 Elasticsearch 的 rescore 机制，根据 BM25 与向量匹配的得分对初步召回的结
果进行重排序，找到最终排名靠前的文档，并打分后返回给前端。

用户输入查询内容并点击搜索

查询参数
发送到 hybridSearch 接口

了

SearchController 接收请求并解
析参数
query、topK、userld

YY

调用 searchWithPermission

提取用户数据库ID与组织标签
用于权限过滤

六

HybridSearchService 调用
EmbeddingClient
将自然语于转为向量

v
构建Elastcsearch 混合检过请
求
[           四
过泪 《公开/本人/组织标
语义检索 (KNN 向量匹配)         关键词检索 (BM25)          Ce

[ 瑟7

Elasticsearch 执行检索并使用
Rescore 重排序

y
对结果打分并封装返回对象

了
返回结果给前端，用户看到检索





## 第 2 页


结果

。 什么是KNN? httpsy/wwwelasticco/cn/whatisenn
。 什么是BM25; httpsyfwwwelasticco/cnrbloglpractical-bm25-parLz-the-bm25-algorithm-and-itssvariables

备注:

kNN 又称 k 最近邻算法，会使用临近度来将一个数据点与训练时所使用并已记住的一个数据集进行对比，从而做出预测。
其中字母 k 表示在分类或回归问题中所考虑的最近邻的数量，NN 代表 k 所选数字的最近邻。

面试时可以这样回答: kNN 是 Elasticsearch 的一个向量相似度搜索功能。它允许我们搜索 内容语义而不仅仅是.关键
词一比如用问题匹配知识库答案，本质是让搜索引擎具备 联想的能力。

想象传统图书馆用关键词查书 (8BM25) ，而 kNN 像一位懂内容的图书管家:

。 内容转密码 (Embedding) : 管家会反每本书的核心思想文本 / 图片/ 音频) 翻译成一组数字密码 向量) ，比如《三国演义》
可能编码为 [8.s，-e.2，8.3， 1

。 相似即分近 《向量空间) : 内容相似的书，数字密码在坐标系中的距离越近〈比如《水洲传》 靠近
学))。

。 按醋离推荐 (kNN 查询) : 当你问: “找和《三国演义)》 风格类似的书"，管家立刻在坐标系中锁定离它最近的K本书 (k=5 就是
找最相似的5 本)。

演义)，远高《量子力

INN

。 Exact KNN: 暴力计算目标向量与所有向量的距离，语法上用 knn 查询 + vector 字段。
。 ANN (Approximate Nearest Neighbor) : 使用 HNSW 算法 (分层导航小世界 建立向量索引，语法上在创建索引时定义

dense_vecter” + indexrs troe

7/ 示例: HGM 于引证义

。 7/ 风量基型
7/ 准时《尖与本型匹配)

adlarttyra neosiner

BM25 是 Elasticsearch 的默认搜索评分算法，它的核心任务是 判断文档和搜索关键词的相关性。可以把它想象成一个公平
的裁判一不仅看关键词出现次数，还要看关键词的含金量-，同时防止长文档作牌。

。 关键词在 当前文档 出现次数越多，得分越高。

。 关键词在 所有文档中越可有【比如"量子计算机"vs “的") ，合金量越高，得分越高。

。 悉别长文档灌水 一 比如'区块链'在 10 页的报告中出现 5 次，比在 100 页的教材中出现 5 次更可信。

综述: BM25 是 Elasticsearch 默认的相关性打分算法，充当了一个很圈明的裁判一用三把尺子来量文档: 词频 (TP)、关
键词含金量 (IDF) 、以及文档长度惩罚机制。比如搜索 苹果手机 时，它会优先选取焦主题的短文，而非泛泛而谈的长文，
同时抑制堆砌关键词的行为-

相比旧算法，BM25 通过参数 k1 和 b 平衡了词频侈和度和长文档干扰，让结果更贴合用户意图。简而言之: 它让搜索引擎
从数数.进化到了.理解内容价值。

2.这和我们平时用的百度搜索一样吗? 还是有什么特别之处?

 有相似之处，但又不太一样。我人这赛搜索远辑，可以理解为在传统的关键词搜索的基础上引入了语义理解的能力，它既保
留了像百度那样的关键词匹配机制，也融合了向量检索这种更智能的语义搜索能力。

当用户发起查询时，系统首先会通过 embedding 模型把这句话转成高维向量。这个向量可以理解为这句话的"语义特征
后续我们会用这个特征去做 KNN 检索，找到语义上相似的文本块-

知识库检索                             x

ak [瑟      ma FEE                cm [SS

ES -机作刘术机伯vn标是TRAE2 0全新升级，开发又于到了正文大家好我是二再晰。7 月 21 日，TRAE 2.0 re
得困半，这次失重大的有就是，A| 不两只是1DE 的一个名工具，他开始具有一症的自主意识，能名所我人的自角和让拓

务，并且是全生命基的。这次新增的 Coding Agent 名为 SOLO，官方地的定位是【The Frst Context Engineer] ，主打一个自动折任务、
自主肖配工具，实现从需求分析到应用印轩的全人开发程。从我的实际体验来看，这地对是一次重量级更新，主打一个折话完成一项复杂的
天用任务，攻比如说，我检开发一个阅历生成上内，我这一名话:请我开发一人简历生成工具。它训开始大二二、到动及地二笑了，他人和
万一从PRD 文档，得计完于的产品划和功能设计，并且可以在右人实时对，丰下地所见有所得。可玖是，他人。
的次全以前，我们开必一
是一个非常和通的技术

来源: 珊对王二-本件刘术docx

机 VP 人，用用，只有条有                       ED

来:wangerbdt




## 第 3 页


当然，光有语义相似度还不够，我们还引入了 BM25 的关键词匹配。因为有些时候用户会输入一个专有名词，或者组织内
部的一些术语，这种场景下，关键词匹配反而比语义更准。所以我们是:    去做初第，再通过 BM25 的 rescore 对
结果再排序，这样能把那些"语义匹配 + 关键词命中"的高质量结果排到前面。

除此之外，我们还做了权限控制，比如说用户查询的文档必须是在用户所在的组织标签下，或者必须是公开的资料，或者必
须是本人上传的文档等等，          我们会通过 flter 的方式一并传给 Elasticsearch，避兔返回用户无权限查看的内容。

整体来看，传统搜索信向的是你说了什么，我就找什么"，而 RAG 希望做到的是"你没说，但你想表达的，我也能理解并找
< 到。

一国

一下=一一外

Data Famework
Ms

四

rentEmbeddng

内
oa

从架构角度讲，这种混合检索也是标准的 RAG 方案。
3.为什么要用'混合检索'? 只用关键词或者只用语义，各自有什么局限性吗?

关键词检索，比如传统的 BM25，它的优势在于直接、高效，适合处理那些比较明确、规范的查询词，比如产品编号、错误
码、ID 这类内容。但它的最大短板就在于，缺乏语义理解能力。打个比方，如果用户输入的是-如何成为一名优秀的程序
员"，而知识库中某篇文章的标题是-提升代码质量和工程能力的实用技巧'，虽然两者语义高度相关，但由于没有关键词上
的明显重合，传统的关键词匹配就会完全错过这条结果。

Le
ov人Ge ) iveva 人 sq Term space   auema
aa ULD -D

Document xd

相反，语义检索怡好解决了这个问题。我们通过豆包的embedding 模型，把用户的查询和文档内容都向量化，再通过 KNN
进行相似度检索。这种方式就不再局限于字面匹配，而是看内容`表达的意思-。哪怕用户问的内容在文档里并没有出现，只
要意思接近，系统也能精准地召回相关文档。但是语义检索也有短板，比如对一些专有名词、产品型号、精确代码这类内容
不够敏感。它会倾向于返回 "意思差不多-的内容，而不是"完全匹配"的结果。

Vector search data workflow





## 第 4 页


从aiastic

介果，二机的 VP 于友们，男的昨，妇的半，只有二天是条多

末尖wangerbxt

疯了王二-各件审村各件vi标题TRAE2 0全新升级，开发帮又到了正大家好，舌是二时。7 月 21 日，TRAE 2.0 全新和级

章，这次近我和大的中就是，A| 不再只是 DE 的一个妈工具，他开妈具有一定的自主人识，能名所我人的自钛许去自理

目是全生命其的。这次新的Coding Agent 名为 SOLO，官方失他的定位是【The Fist Context Engineer] ，主打一个人自动任务自主调

本工具，实现人办来分析双应用部和的全全路开发半程从委的实体验来看，这地于是         个有人     机复杂的开发任
具        基地二适了，他人生成从

务，避比如说我可开发一个半生成工具，我就这一名笑: 请我开发一人
文档，旬信守时的产品夫和功能设计，并且可以在三实时下
项，我们开必一个网站，要先研然后设计 U，失着寺技术方案，建放建表，之后攻寻项目人
非常牛天的技术

得可丽是，他页等的比我写的要更受老板的欢
测试，部时生产环境就和是一个

来:页了王-入件审核docx

4.topK 参数是用来做什么的? 系统是按照什么标准来排序的?

topK 用来控制最后返回给前端的结果数量默认是 10 条。但真正有意思的是，这个 topK 并不是简单地-查10条就结束-,，
是经过了两个阶段的混合排序流各

第一阶段是向量召回，主要看语义相似度。举个例子，如果用户问的是-如何提升工作效率*，那即便知识库里没有完人一样

的问题，但只要有文章讨论的是"时间管理技巧-或,效率工具推荐"，因为它们在语义空间上很接近，也能被召回。这一步我

们一般会放宽范围，比如召回 topk 的 30 倍， 这样能最大化地殉关语义相关内容-。

SearchResponse<EsDocunent> response =

s.index

quen
KK

query(      rq -> rq
queryWeight
YWeight(
rqq -> rqq
fieLd(
query(

rator(0perator

二阶段: 关键词重排。我们会对上一步的结果再跑一次 BM25 的关键词匹配，这时候就把那些既有语义相

接下来会进入
关，又在字面上出现了关键词的文档往前面排

最终，系统会根据这个综合得分，也就是向量得分加关键词得分的加权结果，排个序，然后返回 topK 条给前册。
5.你用什么工具来实现这个'混合检索'的?

我用的是 Elasticsearch。





## 第 5 页


Retrieval augmented
generation (RAG) in action

体alastic

选择 Elasticsearch 的主要原因在于它的开箱即用。它不仅是一个非常成熟的用于关键词匹配的全文搜索引擎，也支持用于
语义相似度匹配的向量检索。

6.详细说明混合检索的实现原理.…各自的权重是如何确定的?

我们并不是简单地把关键词检索和语义检索这两种方式"各自执
方推荐的"召回 + 重排序(Recall + Rescore) "两阶段第咯"

，然后把结果合并一下"。我们用的是 Elasticsearch 官

第一阶段，我们会通过向量检索先从知识库中-捞出一个比较大的候选集，比如是 topk 的 30 倍。这个阶段的目标很明

意思精不精准，只要语义上   问

这种-先召回、后重排-的方式，上既利用了向量检索的广度，又利用了关键词检索的精度，并且性能也很高。

权重调整这块也非常灵活，向量查询我们咎定为 02，重排序查询我们暂定为向量查询的 5 倍，这样做可以保留一部分向量
分数，同时，可以防止那些虽然语义高度相关但关键词匹配各差的优质结果被排到后面。从大量的检索结果来看，目前这个
权重是比较符合预期结果的。

.aoeryeaiaht(e.2a)           才权和
resceretueryetght(1 .6)

7.如何评估你这套混合检索的结果质量?

首先，我们需要构建一个评测集，每条样本会包含一个用户的查询，一组我们人工判定高度相关的文档，以及一些不相关的
文档作为干扰因子。

知识库检索                                                                                                               x

topk    十 关于。 天有明了名加               ma

aroonnin_T57mwnneea nan xw0sxsTxereenrneeoe hu和 间
的间伐，企业各村了大量文档资料，但传的文人管理方式导玫知识息现和天年，员工淮以快纪大信息，严重影响工作和
Apache Ti 文档析、DeepSeek 大横向量化和Elasticsearch 人要技术，和构建了一守完的和能锐识外理光水续。系二外动有
word、PDF、Excel 等多各文档，将非擂构化信息转化为可栓雪的知识责产。更旦要的是，基于语义理的向量检索技术突丰了传人亲本的
局限，用户司以用自枯语言二未系统能名理楚明并过相关内容-

末天paismartpaf

委有一个大和的如法，往后的日了里,所
目了最人和的AIIDE。对于我这种有强迫的人来说，丰的是一种折靖，哪怕和开通了会员，和和能休天扯一理天，也无济于事问题没有骨
机的条怪，因为人本身是没有则题的，实际上只是 Lombok 这块疫有处理好而已。但人我想不开的是，在 TRAE 中打开，同的代码，娄不全有
这和的名失示。我只能说，字节丰的有在认真组AI1DE 这件事，委人细节，得计意产品体验确实本得上我区的 Pro 会员。也再次间所有外提高
技术的小忆们，真的和有几要下载体一下，下和网址: wiirae al，你会有和我的概，和 SOLO 一起术程，丰的太上油了。拉下来，我以
壬一疯芝大家吕有一下用精进行 【六押生工具开发的少快和比武统的包方式，丰的是天之呆，寺最，太和明了。 【好忆展示] 第一
纱，新建一个名为

接着，我们通过这些维度来评测检索的结果质量-

。 看 topk 里面真正相关的比例;

最后，可以做一个 A/B 测试。比如说放出一个新的实验版本，让 5% 的用户使用新版搜索策略，其他 95% 继续用旧的，然
后对比两组用户的行为数据

。 点击率: 新算法返回的结果是不是更吸引用户去点击;




## 第 6 页


引Mapping里都定义了哪些关键字段?

我人的素引名为 knowledge_base，其核心字段大至可

8.你的Es索

内容字段、向量字段、权限字段

先是内容字段，我们定义为 textContent，它的类型是 tedt，底层配的是 ik 中文分
的，像 BM25、match 查询都是作用在这个字段上

关键词的时候，匹配的召回率和准确率会比较好，

蝇。
4中文分词器，我们能把一句话拆得比

持关键词检索
，这样用户用自然语言输

然后是向量字段，名叫 vector，我们定义为 dense    有     ，主要是因为我们用的向量权
2048 维 embedding 模型。这个字段是用来做语义检

当用户发起查询时，我们也会把查询的内容转成向量，再用 KNN 算法去比相似度.

是豆包的

做embedding，然后存到这个字段里

接着就是权限相关的字段，这是我们在做 B 端多租户场景时非常重视的

spublic

主要有三个字段; userld、orgTag 和

。 userld 是 keyword 类型，标记;
同样是keywoi

。ispublic是boolean 类

于哪人用户，用于实现私有文档的权限阿离
类型，表示文档所属的组织标签，用来控制组织内的授权访问

标记文档是否对所有人公开

orgm

我们在

行混合检索的时候，不但可以对 textContent 做关键词匹配、对 vector 做语义检索，同时还能在查询

serld、orgTag、isPublic 等条件

s 窗玛”-X GET “[https://localhost928/knowledge_base/_mapping
  locali    ge_base/_mapping") | jq /命令获取索引中的字段信息

MX GET "https://tocathost:9200/knowtedge_base/_mapptng” | jq

Tine                           rent

168 4

{
nowLedge_base
mapptngs: {
properttes
chunkId

Weyword": 《
type”: “keyw
itgnore_above': 2

基于你的E     Ledge_base的Mappi
主要字段分析
1，核心标识字段

td (text + keyword): 文档唯一标识和           确匹配
chunkTd (integer): 文本块ID，
fend5 (keyword): 文件

内容字段

textContent (text)
vecter (dens

权限控制字段

userId (keyw        0        高和权限控制
orgTag (ke            用于多租户场
isPubttc (bootean): 是否公开字段

pubttc (bootean): 公开标志 (

4 ，版本管理字段

。 modetverston (keyword)





## 第 7 页


9.假设你的检索接口响应很慢.…你是如何一步步把它优化的?

当发现搜索接口变慢的时候，我通常会从三个层面去排查;Elasticsearch 本身的查询效率、服务疯的代码逻辑，还有就是
能不能加缓存。

Elasticsearch 是影响性能最关键的因素。如果 ES 本身查询慢，上层再怎么做都救不回来。我会先看看索引设计是否合理，
比如有没有设置太多字段参与检索》 有没有字段类型设置错了? ES 占用的内存是否足够用?

第二层是代码远辑。我会重上
算? 是不是每次都查了太多字肛

ES 的查询语句写得是否优雅，比如能不能用 flter 瞪代 query 来避免不必要的相关度计
返回?

最后就是缓存这块。我会针对一些高频的查询，对 embedding 结果做一次缓存，六免用户每次查询都要重新跑向量模型-
更重要的是，对于一些热点查询，可以把 ES 返回的结果整体缓存下来，这样后续相同的查询请求可以直接从缓存返回，完
全绕过 E5，从而减轻 ES 的负担。

10.我们现在进入检索的核心。假设知识库里有成千上万份文档，分属不同的人和

部门。当一个用户 (比如'王二') 发起搜索时，你是如何确保你的检索逻辑，从一
ea 请详细描述一下这个权限过滤的实现机
。

我当时的设计是，查询必须在 ES 这里就进行权限过泪，这样做的好处是，在进行检索的时候，
直接就把没有权限的结果过滤挤

到用户有权限的

当用户上传文档的时候，我们就把权限写到 ES 中; 当用户进行查询时，我们直接把他的权限写进 ES 的 filter 字句中，这样
既不会影响排序相关性计算，ES 也能对 filter 的结果做缓存。

比如说当一个用户 (比如'王二") 发起检索请求时，请求会的带他的身份      (也就是JWT Token) 。后端在收到请求
后，会解析这个 Token，拿到用户的 userid，再从 Redis 缓存中取出用户的组织标签等信息。

接着，我们会将用户的身份信息，和组绍|
条件，就被认为用户是有权访问的:
。 该文档是公开的 (isPublic tue)-

。 该文档的创建者是当前用户 (userld:"王二的 userld")
。 该文档的权限标签 (orgTag) 与用户的权限标签有交集。

带入到 ES 的查询语句中，我们的逻辑是: 一个文档，只要满足以下任意一个

我们在查询时会把这三个条件都加进去，ES 就会自动进行过滤。

caouery -nes Booltuery .8

poleuery -Builder permls

Permtssionloolguery -sheuld(s -steratt -》tHield(rispublic-) -valueterue)))3

Legtnuser loginuser - Userutdls .Beateginuser(;
Sf (Objectsnonlulltloginuser))

permtssionBoolguery -should(s -》s.teratt -tfield(ruserIdr).valuetloginUser_getuserIa0))))
Wuttls.tskotEmpty(logtnuser getorgTags())) {

engoolguery .shouldfs -》 sterestt ->上
eeraTa

armstnan TearssueryFleld .Buildar()valoa人
logtnuserBerOrgTags()-streas().maptFieldvalue:ief) -collecetcellecters.tottst()
Jeataa0)

atnBoolguery ,filter(f -，和.booltperatssionoolouery -build0)))

ndex(esConfig.petIndexllane()
ery(q -> qboelmadnBoolguery build0)))

ExnovledesBasevclass)i

11.为什么先用ES，而不是直接用FAISS?
之所以选择使用 Elasticsearch，而不是直接用 FAISS，我也思考了很多。

首先，我对 Elasticsearch 比较热悉。像派陪明这种企业级的 RAG 应用，除了向量召回以外，还会涉及关键词匹配、权限过
滤等,这些用 ES 都能一站式解决。比如我们用 dense_vector 字段配合 HNSW 做语义检索; 关键记检索部分用的是
BM25; 而像权限控制，我们可以直接在 ES 查询中用 filter 语法，对 userld、orgTag、ispublic 等字段做前置过滤，不需要
拉数据回来再用 Java 代码做后置处理，

Retrieval augmented
1         人





## 第 8 页


侈aastic

另外，从运维和研发成本上看，Elasticsearch 生态非常成训。

当然，FAISS 本身也是一个非常优秀的向量搜索引擎，它的算法性能和内存效率都非常强。下一个版本我是打算用 FAISS
的。

12.你的ES索引是如何处理高维向量的? 有哪些特殊的配置?
首先是向量字段 vector 的定义，它的类型为 dense_vector，明确告诉 Hasticsearch，这个字段是用来存储密集|

Seorch query

ED

um             mbeaane orerior               ee wan                   eerson
prodoenn wereal 。 pt Tanalormer Modal ed

ae

这是使用 Elasticsearch kNN 搜索的基础，只有定义为 dense_vector 的字段才能用于后续的 knn 查询。

维度和我们使用的豆包 embedding 模型对齐，都是 2048 维-

存储单个向量所需的空间越大《2048 个 float = 2048 x 4 bytes = 8KB) ，构建索引和进行 KNN 搜索的计算开销通常也越
大。

然后，我们会设置ndex 等于 true，要求 lasticsearch 为这个向量字段构建专门的近似最近邻 (ANN) 索引。

对于包含海量文档的

，进行精确的 kNN 搜索是不太可行的《时间复杂度为 OUN)。
构建 ANN 索引是为了实现 亚线性时间 的搜索-

昔后用到的是 HNSW 算法，该算法会构建一个多居图

entry point





## 第 9 页


一 一

搜索的时候会从项层开始《节点少) ，然后快速定位到目标区域。然后下降到下一层更密集的图中，在更小的候选区域内进
行更精细的搜索。重复此过程直到最底层。在每一层，算法都会沿着连接边 (代表向量在空间中的邻近关系) 向查询向量方
向-全心地移动-

该算法的优点是在精度和召回率 《找到真正的最近邻) 与查询速度之间取得了非常好的平衡，并且相对容易调优-

Elasticsearch provides al
three key capabilities in one
platform:

(A) nearest neighbor search,
(B) inference APl and

(C) embedding model

OpPyrorch             Caon orEmbeadngs

日。 Execetion of vector Search

还有一个非常重要的参数 -similarity": "cosine"，指定在构建 ANN 索引和进行 ANN 搜索时，用于衡量 向量之间相似性 (或
距离) 的度量算法。我们选择的是 余弦相似度，只关注向量的方向，忽略其大小长度) 。

这对许多语义搜索任务特别有利，因为文档长度不影响相似性判断(类似于 BM25 的思想)。

13.在大规模场景下，ES会存在哪些性能瓶颈?

首先是内存拒预，HNSW 图结构需要完全加载到 JVM 堆内存中。随着向量数量和维度的增加，内存消耗会急剧上升，成为
单个节点容量的主要限制因素。

eee 于呈     @@Q@Oe cu卫久网洛 Q

进程名称                                             本“      和 区DPID 。 玫户

国 meuu peA                           3.01GB     281    987 。 35106 。 itwanger
国 jav                            1.6268     43 240 59830 ianger
国 jw                               1.5268      43 240 22578 imanger
全全                             .005.1MB      7 745 4055 wanger

oogle chrome Helper lepu)                                               7473MB              22 492 4209 imanger

其次是 CPU 拒预，k-NN 查询是计算密集型任务。在高并发查询场景下，大量的距离计算会消耗巨大的 CPU 资源，导致查
询延迟增加和吞吐量下降。

羡                                          0512   评论
热门评论 区
区 kzam                                         02
| 引用大文: 关键内分的是Bh25
党明,向量检过和关键订检是同时进行的史” topk是什么时人用的
丰s 国B
aa                                            sosmn28

向量检索和关键词检索不是同时进行的，而是采用"混合检索-的两阶段策略-

工第一阶段(如回): 先使用向量检索 (如KNN) 从知识库中召回一个较大的保选集，通常是topk的30信左右。这一
阶段主要看语义相似度，确保不沁挤任何可能相关的文档




## 第 10 页


了 第二阶段〈重排): 对召回的候选集使用8M25进行关键局匹配和重排序。这一阶段确保结果降语义相关又包舍用
户坦询的关键词-

topk参数的使用时机
。 在第一阶段，系统会召回比最终topk多得多的候选结果(如topK的30信)

。 在第二阶段，系统会对这些候寺结果进行HM25重持序

。 最终，根据向量得分和BM25得分的加权综合得分，选出前topK条结果返回给用户

这种"先召回后重排”的方式结合了向量检索的广度(能找到语义相关但关键词不完全匹配的文档) 和关键词检索的精
度 (确保结果包含用户查询的关键沁) ，提高了检索的准确性和全面性-

出s介 加旧虽

条 zamm                                              ossoamnzB ia
鲍派聪明，使用knn向量检索的时候会进行权限过滤吗

向量检索和关键词检索不是同时进行的，而是采用-混合检索-的两阶段策路; 1 “第一阶段《召回) “…: 先使用

攻 用上

]                                                                                     026和0月12日342

是的，派脱明在使用knn向量检索时会进行权限过小。系统通过在Elasticsearch查询中添加权限过站条件来实现这一
点，确保用户只能访问其有权限查看的内容。具体来说，系统会"文档是否满足以下任一条件:

十文档是公开的[spublic true

2 文档属于当前用户(userid匹配)

3 文档的权限标签(orgTag)与用户的权限标签有交集

这种权限过渤是在FS查询的fiter子句中实现的，既不影响排序相关性计算，又能让ES对过泪结果进行缓存，提高检索
效率。

@涛聪明，使用knn向量检索的时候会进行权限过关中

六 用回和

条 samm                                              nasalz aa
加派聪明，那在bm25关键词检索的时候还会进行权限过滤吗

是的，泊有明在使用knn向量检索时会进行权限过泪。系统通过在Elasticsearch查询中汪加权限过小条件来实现这

竹用上

]                                          026和08月12日2349
是的，在BM25关键词检索时仍然会进行权限过汛。根据检索系统的设计，权限过站是在Elasticsearch导而就进行的，
而不是在检索完成后才进行过泪。
具体实现方式是:

工当用户上传文档时，系统会将权限信息《如public、userld、org sg等) 写入E5素引

2 当用户进行查询时，系统会持用户的权限信息写进ES查询"“的fiter子名中

3.E5会根据权限条件自动过所撞用户无权访问的文档，只返回有权限的结果

权限过沪条件包括:

文档是公开的spublic tmue)

文档创建者是当前用户(userid匹配)

。 文档的org Iag与用户的权限标签有交集

这种设计的好处是既保证了安全性，又不会影响8BM25的相关性计算，同时ES还能对filter结果进行缓存，提高查询效
率.

@涛肌明，那在bm25关键订检索的时候还会进行权限过小吗

总遇 明加和

10条评论

Gorou                                                                                           026年月13日 1901

引用原文: 青后用到的是 HNSW 算法，读算法会构建一个多层图。

新增向量节点的过程:
1 会先将自己作为查询目标在多层图中跑一遍，找到每一导与之最相似的向量;

2 决定该新节点的高度，类似中表的每县2概率;

3 在自己的最高层，从查询时的最相似节点开始，寻找不那么相似的名居相连，并依次往下;

@源明 锐评一下上述观点是否正确-

而点革”国卓1

]                                          026年9朋13日1902
根据参考资料中关于HNSW算法的措述，用户新增的节点过程措述存在部分正确但关键细节有误的问题，以下是逐点|
分析:

1 "先将自己作为查询目标在多层图中跑一读，找到每一层与之最相似的向量
全




## 第 11 页


 。 原因: 新增节点时无需执行查询操作。HNSW的过程是构建加结构~，而非检索相似节点-
正确运辑:

新节点时，需从最高层开始*”(随机决定导数) ，在每一层中

从更有久居中陋机选择一个节点作为起点;

在该节点的邻居中寻找与新节点中离更近的节点，逐步更新连接

， 直到无法找到更近的邻居，则将新节点加入当前层的邻居列表-
(堆考资料中趟提及-作为查询目标-的步圣，读措术混汪了查询与操作)。

2决定该新节点的高度，类似跳表的每层1/2概率"
回 正确

。 傅据: HNSW采用层级概率分布 加跳表) ，高层节点概率更低。例如:
， 第0居 《最底层) 概率为1;
。 第1展概率为112;

第2层概率为114，以此类推。
(大考资料中提到多层图"'，上且层级设计与跳表一致)。

3在自己的最高层，从查询时的最相似节点开始，寻找不那么相似的邻居相连
X 错误
原因
， 查询时的最相似节点"操作不信吉查询结果“…，而是从随机六居开始这历;
 。 号找不那么相似的免居"… 实际目标是连接更相似的节点 (缩路径) ，而和"不那么相似

正确远辑:
在每一层时，新节点会优先连接下离更近的久居*…，以优化查询效率。例如:

区

风机二个儿加

5asaghpeslentry patae-aadahpora，aeda)
1 aaaranee aeda，eandtdare) < daarancelaode，encry Petatl

和有node加Nonsry point的j加表
Cr eeeeeeeeseeeeeeeeeestaoaey

复制代码

总结: 错误的核心问题

1 混清查询与扣作…
“时无需"作为查询目标，而是直接构建图的连接关系。

2 邻居连接逻辑错误

实际目标是连接更相似的节点《优化查询路径) ，而非-不那么相似-
了 缺少随机选择机制

真实过程信厅随机选择久居"…作为起点，而非国定从"是相似节点开始。
参考资料依据

。 HNSw通过属级概率分布移定节点高度 (下表机W) ;

， 时在每一层随机选择旬居起点，并选代寻找更近的节点"构建连接;

 。 目标是和于高效连通图，而非记齐连接-不那么相人-的节点。

加四

千 :xmm                                              2ozcsoa128 2253

引用原文: 关键词检索部分用的是 BM25

@泥聪明，向量检索和关键词检索是同时进行的吗?topK是什么时候用到的

5妆”国占85

天明                                                                                              026和0月12日2255
向量检索和关键词检索不是同时进行的，而是采用-混合检索-的两阶段策略:

十第一阶段(召回): 先使用向量检索 (如KNN) 从知识库中召回一个较大的候选集，通常是topk的30人左右。这一
阶段主要看语义相似度，确保不沁掉任何可能相关的文档

2 第二阶段《重排) :对召回的候选集使用8M25进行关键词匹配和重排序。这一乔段确保结果皮语义相关又包合用
户查询的关键词

topK参数的使用时机

在第一阶段，系统会召回比最终topk多得多的候选结果(和0topk的30信)

在第二阶段，系统会对这些候选结果进行BM25重拓序

。 最终，根据向量得分和BM25得分的加权综合得分，选出前topK条结果返回给用户

这种"先召回后重排”的方式结合了向量检索的广度【能找到语义相关但关键词不完全匹配的文档) 和关键词检索的精
度 (确保结果包含用户查询的关键词) ，提高了检索的准确性和全面性.

说昌明加和





## 第 12 页


区 :zam                                              aagoaanza az
 @尖本明，使用nn向量检案的时候全进行权限过过中

向量检索和关键词检索不是同时进行的，而是采用"混合检索-的两阶段策咯: 1 "第一阶段(召回) "*: 先使用

六时 国旧和

人                                                                                     026年09月12日 342

是的，派脱明在使用knn向量检索时会进行权限过沾。系统通过在Elasticsearch查询中添加权限过汉条件来实现这一
点，确保用户只能访问其有权限查看的内容。具体来说，系统会"文档是否满足以下任一条件:

工文档是公开的(sPublic true)
2 文档属于当前用户(userld匹配)
3 .文档的权限标签(orgTag)与用户的权限标签有交集

这种权限过污是在ES查询的filter子句中实现的，既不影响排序相关性计算，又能让ES对过外结果进行缓存，提高检索
效率。

@涛聪明，使用nn向量检索的时候会进行权限过小吗

总时 国回提

徊 :zam                                              oawoaanz aa
@@派聪明，那在bm25关键词检索的时候还会进行权限过滤吗

是的，派聪明在使用knn向量检索时会进行权限过沪。系统通过在Elasticsearch查询中生加权限过泪条件来实现这
用 加和

5                                                                                     026年月12日349
是的，在BM25关刍词检索时仍然会进行权限过扎。根据检索系统的设计，权限过小是在Elasticsearch导面就送行的，
而不是在检索完成后才进行过涉。
具体实现方式是:

工当用户上传文档时，系统会将权限信息 《如Public、userld、org sg等) 写入ES素引

2 当用户进行查询时，系统会棕用户的权限信息写进ES查询"“的fter子句中

3 6会根据权限条件自动过尖撞用户无权访问的文档，只返回有权限的结果

权限过尖条件包括:

文档是公开的spublic tnuel

文档创建者是当前用户(userid开配)

。 文档的orgTag与用户的权限标签有交集

这种设计的好处是取保证了安全性，又不会影响8M25的相关性计算，辣时ES还能对fter结果进行缓存，提高查询效
于。

@涛聪明，那在bm25关键词检索的时候还会进行权限过湾吗

026和0月12日202

混合引擎搜索部分的代码，是先扩大30信的范围进行KNN算法得到禄步检索结果，在这一步的结果上继续进行BM25计算，再
进行权限过尖，得到的结果后进行基于BM25的重排序吗?

// 必须命中关键词 + 权限过涉
ee ->
musttmst -> om -> oo人textContent)querytquery)
hiterf -> we -> bf部分代码是在进行8M25算分吗
出点兴国加三
备 kzam                                       ozcsoat2 ta4

引用原文: 当一个用户在搜索权时输入一句话然后点击搜索，系统大臻全经历一个怎样的处理流程

@尖聪明，这个问题和"详细描述一下，从用户狂入一个问题，到系统给出回答，整个RAG流程是怎样的-这个问题有什么区虽
而点革 国旧1

1]                                                                                     026征08月12日1134
这两个问题的区别主要在于关注点的广度和深度不同:
第一个问题 (搜索流程专注于检索系统本身的工作流程，主要关注:

。 用户答入到到回搜索结果的步李

。 系统如何处理查询请求

。 Easticsearch混合检索的实现 (KNN+BM25)

权限过污机制

而第二个问题 (RAG流程)则适盖了更完整的员到员流程，在搜索流程的基础上还增加了:
。 检索到的内容如何与大语言模型 (LUM) 结合

。 提示工程 (prompt constuction) 环节

LUM生成最终回答的步村




## 第 13 页


至个-检票-增强-生所-的闭环滨程

简单来说，第一个问题是-如何找到相关信息"，第二个问题是-如何找到信息并生成答案'。RAG流程是搜索流程的拉
展，增加了生成环节，合系统能从单纯的信息检索升级为因能问答

总1 用四三

图 ammes                                                        oo6生03月1日 2056
引用原文: 首先是内容字段，我们定义为 texContent，它的美型是text，底层配的是 交中文分词蝇

底层配的是k中文分沁吕这里，这个分词器是怎样起作用的@深脱明
而1 国四1

]                                          oz6和0月1 2057
ik中文分词器在Elasticsearch中的作用机制知下《结合参考资料中的配置和中文搜索特性)
工 核心功能: 解决中文分词难题
。 中文无天然空格分古，需桂连续字符切分为独立词语 (如“我有编程。 "我"，"委"， "搞各-1 )。
?内词器通过词册匹配+统计模型实现暂能切分避免英文空格分词对中文的无效性-
2分词模式选择

ksmart (得粒度)
适合标题/短文本，保留核心语义(如 “rr 一

kmaxword (细粒订):
合内容检索，最大化切分 (如 “sse 一人est
国"1 )，提升召回率.

3 对搜索性能的影响

索引阶段:
文档内容经分词后存入个排索引(如 “execcatent”字段)，每个词关联文档|D。

查询阶段:
用户查询同样分词 如搜索 “…*手机” ~，["*…，*手机") ) ，匹配素引中的词组，提升BM25算法的词频统
计准确性.，

4 与8BM25的协同作用

。 BhM25傅和分词后的词频计算相关性:
， 着文档含 “…科手机”，ik_max word能识别 “……”，确保BM25正确统计词频。
，* 若未分词，…*手机” 可能和被当作整体词，导至与“…朋手机”无法配.

.配置示例 (参考资料中的映射)

ssera" /1 使用:8本

ik分词器是中文搜索的基石:
加 切分连续字符 - 构建可检索的词单元

支持组粒度分词 - 提升BM25召回率
加 与Elasticsearch深度集成 -无需阁外处理即可用于关键记检索 (30BM25)

i 国旧三

四本                                                           ooesoaBo6B to

引用原文: 我们用泗用 Elasticsearch 的 rescore 机制。也就是说，我们不再对所有文档做关键词匹配，而只是对刚出召回
的那一批候选集

@尖聪明，第一阶段用bm25与nn召回，是先进行knn 召回30们topk 再关键词过污，第二阶段还是用bm25 重排吗

出贞时 国日23

]                                                                                     026和0有06B 1700

根据提供的参考资料，混合检索的两阶段流程如下:
第一阶段: 语义召回 (KNN)

3 目标: 最大化本盖语义相关内容，不依未关键词配
， 通过 Embedding 模型村查询转为向量，在 Elasticsearch 中执行 KNN 查询
， 召回结果包合语义相似但无关键洒重熏的文格 【如-提升工作效率-匹配'时间管理技巧)

第二阶段: 关键词重排 (BM25 Rescore)
直方式: 对候选集使用 BM25 算法重排，而非全量文档
， 通过Elasticsearch 的 rssccrs 机制实现
， 仅对第一阶段召回的 300 条候选集计算 BM25 分数
2 目标: 结合关键词配优化排序
。 对候选集中的文档进行关键洒相关性打分 (如"…手机"优先匹配合流词的文档)




## 第 14 页


3 最终排序:
综合向量得分 (KNN) 与关键词得分 (BM25) ，按加权结果排序

。 权重示例: auecyieiaht-0.2 (KNN) + rescarecueryeight1.0 (8BM25)
关键区别
的机        全用的技术         作用范图                目标
划一队和       INN 向和检索        全时文档 《通义层面)           广泛召回和义相关内容
第二队抽        Bh25重排            候寺入《关键河民面)             了准拓这关键河区本结果
全 起结:

。 第一阶段: 仅用 KNN 召回候选集 【不涉及 BM25)。
 。 第二阶段: 仅对候选集用 BM25 重排，避锡全量计算，提升效率。
。 通过 -<<c。宙制实现两阶段协癌，羔晤语义广度与关键请度-

由兴国四所

四本                                                              assoaosa maz
回派聪明，所以最后一步是加权融合还是重排

根据提供的参考资料，混人检索的两阶段流程如下; ### **第一阶段; 语义召回 (KNN) "1. “方式:公使用
出si 用四
]                                          oz6年有06B 1704

在混合检索中，最后一步是重排 (Rescore) ，而非简单的加权融合。以下是核心到加的清晰说明:
阶段流程

第一阶段(召回):
使用 KNN语义检索 从海量文档中召回 -px 。30 们的候选结果《例如 cez-10 时召回300条)。此阶段公信
朝向量相似度，确保太半所有语义相关内容,

第二阶段〈重排):

对召回的候选集，通过 BM25关键词匹配 重新计算相关性得分，并普换原有向量得分，生成终排序结果.
2 重排 vs 加权融合

斌              计算范围            得分外理                         效于
重排 (Rescore) 。。 仅候选集 〔少晶) 。。。 完全名措向量得分为BM25得分         加 商政(过免全量计算)
用全             全昌文档 【大出        风量得分 ，BM25得分 (娄学加权)         X 全站 寺归开铺大)

3 关键实现细节

Elasticsearch Rescore机制:

-ceaceret
ceacorecaerylea -> oa
as人 -> eve resxscoatentr， "网章各订-1
Lerymedgh16.2 /1 闪昌分杆 (信放量)
rescorecaeryedghr .0 1/ aas得分醒(主导拓了)

复制人三

结果生成
最终排序完全由 BM25得分 决定 (权重为10) ，向最得分仅作为保留项 【权重0.2) ，确保关键词匹配优先-

4 为什么选择重排?

。 精准性:6M25对专有名词、术语更敏感，避免语义相似但关键词员失的误判-
。 效率: 仅对候选入 (如300条) 计算BM25，而非全量文档，大幅降低计算成本。
。 可控性: 通过权重调整 (如 serywe:she-0.2 ) ，平衡语义与关键词的优先级.
总结

重排是最线排序的核心机制，通过在候选集上重新计算BM25得分，实现"语义召回+关键司精排-的高效结合，确保结
果降可关语义相关内容，又精准玫配用户关键词需求-

@尖聪明，所以最后一步是加权帆合还是重拓

人 om                                                                                            oz6soaRoB 1531

引用原文: 我们会通过向量检索先从知识库中-搬出一个比较大的候选集，比如是 topK 的 30 售。这个阶段的目标很明
确，就是"求全'。不管搜索的内容标不标准、意思精不精准，只要后义上四用户的问题相关，我们就都拿出来，不放过任
一个潜在的好结果，

@派陪明 这里是否存在权限浊沁题。在进行knn时并没有对权限做任何过泪，到关键词匹配时才"…了权限。

有这样一种情况: 如果 KNN 找到了一个极度相似的文章《比如得分 100分) ，但这篇文章用户没有权限看。而 Query 部分没找
到这篇文章。E5 可能会因为 KNN 分数太高，强行把这篇无权限的文章推送给用户!

而吉半加加昌1




## 第 15 页


人                                           2026征09月06日 1532
您提出的问题非常关键，当前设计确实存在权限浊涯风险，只体分析如下:
间题核心
1 两阶段权限控制党
。KNN召回阶段; 仅基于语义相似度召回候选集 (如 topK 的 30 们) ，未进行权限过泪-
NM25重排阶段:对候选集进行关键词配和权限过沾-

一风险: 车 KNN 召回的候选集中包含用户无权限的高分文档(如相似度 100 分)，而 BM25 未匹配到其他符
代广档，该天权限文档仍可能被推送到最终结果中

2 .65 查询执行顺序问是
Blastcsearch 的 =cc; 查询中， :iire= 子句的过外是在 suezy 子句之后执行的《即使 <:1e= 写在
uacy 前面)。

一实际执行吴序:
。先计算 KNN 和 BM25 的相关性得分 (排序依据) 。

， 后执行权限过泪( :1 =。 子句)
一 若训分无权限文档未被 BM25 持名村盖，仍可能法入最终结果。

解决方案
1. 权限过滤必须前置到 KNN 召回阶段

在构建 KNN 查询时，直接移权限条件纳入ite= 子句，确保召回阶段即排除无权限雪据

7/ mm 查划了和要过四

aaeayecrerlqaerywecrorl
aocanataaseatecpr 。30)
-asezlgernasstenPlisezli 1 加 关键 机了过下
复审人到

2. 双重权限验证机制

第一阶段 (KNN) : 召回时过污无权限数据-
， 第二阶段 (BM25 重排) :再次验证权限，确保无站酒

1/
aas leaaooeryshFllsezl 1 IN + 要过下
aaascaery)         7 aas 关外本
acerlpermaasdcagalrezli 1/ 回 列队

复制代到

3. ES 查询优化建议

“使用 Eilcar 而非 ms
权限过尖应使用;ixer 《不参与相关性计算) ，避旬彩响得分排序。

， 组存权限条件:
将用户权限标签(如 “cra3 )】 缓存到 Redis，减少每次查询解析开销

权限泄漏场景示例
有               用户查询:公司时务报告-                                          结果风险
KNN召回 瑟瑟到无权限文档A (相似座 100 分)             X 未过他，进入介入
Bi25 量排。。 未下配时其他文档，A 的 6M25 分数较低但从被保留       X 无权限文档人进入最终结果
修复后的安全流程

有F章询| -> stmey 召回 。 机过渤
过下后人和
-atacs 关说)
-> 8 cp 弛1
aregh 机
间 esasae | 要

当前设计的核心在于权限过滩湾后，导致 KNN 召回阶段可能包含无权限数据。必须将权限过泪前
axer 子名中，并二以重排阶段的二次验证，才能彻底避免权限进沁-

出训8 加加

2026和3有05晶2036

引用原文: 权限过湾机制

@源税明，权限过汉与检索谁在前谁在后
国昌5

人                                                                                                   6和0
根据参考资料，权限过小是在检索阶段之前进行的，只体实现机和0下
工 权限过湾的时机

有05日2037




## 第 16 页


在 Hastkcsearch 全下县区上入和限末怀 【名才贡利于1让)
。 当用户发起楼过请求时，系统会和用户的权限信息 (如 sez13 、 -zsraa 、 :=pubiic ) 直接写入 ES 查询"的 ilcer

子句中。
示例过加:
7/ 本过条件 《在间执行)
eeeeaeeeeeeeeessseesresta -oseste -> oveeeefeaagublioc)vealaetsroeii 11 全下
cstapeeresceeccalN)有1/ 性答
区        ar era legtspeer-gesozgzegs0111 7/ 局让

复制

作用: ES 在执行向量检索 (KNN) 和关键洒检索 (8BM25) 时，公返回符合权限条件的文档，避免检索无权限数据_

2 与检索流程的关系
检索前过湾 (参考资料第10点、第8点)
， 权限过江作为FS 查询的ilzsz 子名，与 query 子句 (包含 RNN 和 BM25) 网时执行，但优先过泪无权限数据,
人
， 性能优化: 减少无效数据的向量/关键词计莽量-
安全性保障-避免略数据返回到应用居.
上5级存利用: ES 会缓存 +i1=: 结果，提升重复查询效率

， 检索后过泪 (不采用)
，老先检索再过涉，需在应用层二次处理，增加下迟和资源消耗，且存在数据泄委风险.

3为什么前置过湾更合理?
。 参考资料第10点明确说明: 权限过站需在 ES 查询中完成，确保检索范围公包合用户有权访问的数据.
。 参考资料第9点提到性能优化时，强调通过:=。= 减少计算量，印证了前置过涉的必要性

总结

权限过浊与检索的执行听序为:

权限过湾 (ES 的“ilter 子句) 一 向量检索 (KNN) - 关键词重排 (BM25)
这种设计确保了安全性、性能和结果的相关性-

总遇虹回复

2026和03朋04日 ad4

引用原文: 随着向量数革和维度的增加，内存消耗会急剧上升，成为单个节点容量的主要限制因素

@尖了明 这里是es中存储的向量增加后，会有内存类纪四?
出点革国卓g

CT                                          026和3有04昌 1945
是的，随着ES中存储的向量才量和维度的增加，内存消耗会急剧上升，成为单个节点容量的主要限制因素。以下是具
体原因和彩响:

内存并红的核心原因

1 HNSW算法的内存开销
使用HNSW (分层可"小世界图) 算法构建向最案引。读算法需在内存中维护一个多层园结构:

。 每个向量节点需存储其连接的邻居节点指针-

， 随着向量数量增加，节点辣的连接关系旺指数级增长 oY 1c9 N) ) ，内存占用远超原始向量数所本身。
， 示例: 百万级2048维向量，仅轩结构可能占用数十G8内存。

最数据的直接存储

， 单个2048维向量占用约 2048 ， 全节 Bra 。

。 百万级向量原始数据需 srs ，;, 000, 000 scs ，加上图结构开销，实际内存需求可能时信-

3JVM堆内存限制
上使蒜VM运行，HNSW率引荐完人加载到堆内存中。若内存不足:

名改频公垃圾回收 【GC) ，导致查询延刀积升
。 可能引发0OM (内存省出) 导致节点贿江。

性能影响

。 查询琵迟增加: 内存不足时，HNSW和项从琵瘟加载部分数据，"内存计算优势-

 。香吐量下降: 高并发查询时，CPU争抢内存资源，降低处理能力。

。 扩展性受限: 单节点容量类锯合增加节点，但分布式同步(如分片间通信) 会进一步站性能.

缓解方案
工优化HNSw参数
。 调整 。* (搜索时候选集大小，和《每人节点的最大连接孝)

Ce
1
得




## 第 17 页


， 权街: 降低参至可节省内存，但可能逢竹召加

了 分片策几优化
， 将向量数据按用户/分片，分数到多个节点，各锡单节点过载_
， 合理设置分片大小(建议单分片<50GB数据)。

3 硬件升级
* 增加单节点内存 (如128GB+) ，并启用ES的堆内存优化【 -xns 与 -xnx 设置)-

人外代方案
。 对超大规模场景，考虑专用向量数据库 (如FAISS、Mikvus) ，它们针对向量存储和检索做了深度优化-

的HNSW向最案引在数据重激增时歼实面临严梭的内存瓶颈，其根本原因是图结构的内存开销远超原给数据，通过
参数调优、分片设计和硬件升级可部分缓解，但极限场景需转向更专业的向量数据库._

出总潮 国四三
图 se                                                        12026年月04 1856
引用原文: ANN

@派陪明 派脱朋在使用KNN查询时使用的是什么方式实现? 为什么?
而点半”国四

Ta                                                                                     oz6s0Ro4B 1856
柱所参考资料，深鹏明在KNN坦询中采用 ANN_(Approximate Nearest Neighbon) 方式实现，只体通过 HNSW 《分
层小世界) 算法 构建向最索引。以下是原因分析:
实现方式: ANN (HNSW算法)

工检引配置
在Blasticsearch中定义向量字段时，启用ANN素引:

aeaataa机度-
17/ 各Ra
Casaaew 11 名用外人度

，。 inaax+ ceus 表示构建ANN索引 《而非…计算)。
，。 similaricy: cosine” 确保向量方向相似性，忽路长度影响-

二算法原理
HINSW通过构建多层图结构实现高效搜索:

。"尹奔…: 从而导各项图快速定位目标区域，再逐层组化到密集层。
。 偶必搜索: 在每有沿着向量久近边 (HNSW图中的连接) 向查询向量方向移动，减少计算量-
，亚线性时间复杂度: 相比Exact KNN的OIN)，ANN格搜索效率提升至Oog NJ。

为什么选择ANN而非Evact KNN?
工性能优势
， Exact KNN 需计算目标向量与所有向量的距离，时间复杂度ON)，在百万级数据下不可行。
。 ANN 通过HNSW算法大幅减少计算量，满足实时检索需求(参考资料: 第12节)。

2 内存与资源效率
。 Exact KNN需全量扫措，内存消帮随数据量指数级增长。

。 ANN的HNSW图结构昌需加载到内存，但通过分层设计优化了内存占用《参考资料: 第13节)

3 精度与达度平和
， ANN在保证高召回率的同时，显荐降低查询延迟(参考资料: 第12节强调其"精度与速度平衡) 。

为什么不直接用FAISS?

一站或解决方案-
Elasticsearch同时支持向量检索 (ANN) 、关键词配 (BM25) 和权限过过，避免多系统协同的复杂性(参考资
笠: 第5、11节)-

工程戌本:
FAISS需额外开发接口和运维，而ES开箱即用，适合企业级RAG场景 (参考资料: 第11节)。

总结
党明选择ANN_(HNSW) 的核心原因是: 在保证语义检索精度的前提下，实现大规模数据的亚线性时间搜索，装闻
性能与资源效率。示来计划引入FAISS进一步优化《参考资料: 第11节) ，但当前ES的ANN方案已满足业务需求-




