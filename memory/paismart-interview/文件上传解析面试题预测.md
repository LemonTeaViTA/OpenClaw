# 文件上传解析面试题预测

> 来源：文件上传解析面试题预测.pdf

> 解析时间：2026-03-14 16:28:46

> 本文档由 OCR 识别生成，可能存在少量识别错误


---


## 文档信息


- **总页数**: 22

- **文件大小**: 34.09 MB


---


## 第 1 页


首页 ”教程 。 派陪明

回派聪明 RAG 文件上传解析面试题预测，19 道，覆盖 Kafka、MinlO、断点续传、分片上传

对于大文件，派聪明采用的是,分片上传 + 断点续传的方式。我们会在前
片，就丰
已经上传，不用从头开始，用户体验会

峙先把大文

Paismart

Explorer

Folder

fitehd5

hunkTndex

totatsize
fiteMane
fite
orgTag
ispubtic

miniopublicun
Uploadchunk(String,i
getFilery
isChunkUplo
markChunkUploaded(
fiteType
getUploadedchun
isBitSettbyte0l,int)

getTotalci                                        罗

0               fiteHd5，chunkTndex，totatSize，fiteMame,

deleteFileMarkf

Timaline
Java projects

Maven                                                 fiteExist

我们会招;
个文件的上传状态。这也是为了方便后续的状态管理和权限控制.

Minlo 中，同时会用 Redis 的 bitmap 去记录哪些分片已经上传成功。这样的好处就是，即使上传过程中断了，前端可以根据 Redis 状态判断

chunkInfoRepository

- con yizhaoqi .smartpai

getFiteType (fiteame:

contentType = fite.getContentType

成小的分片，比如 5MB 一块，然后并发地上传到后端。后端每收到一个分

上

fiteType，contentType，]|

=-fileUotoadRepository.findByFiLeHd5hndUserTd(fiteHd5

个文件的元信息，比如文件名、文件大小、上传者、所属组织标签等，保存到 MySQL 中，用来跟踪束

id               created_at                    file_md5                               file_name             is_pub
并 bigint      @ datetime(6)             hpBc varchar(32)                    hpBc varchar(255)     名 bit(
1 2025-06-30 15:37:57103935 ”2e8d7e452961fa303fe495756fa82174 ”wangertxt            0
3 2025-07-02 15:17:45.871515 ”人f3ba82ec5664767e2c487141fa08a03 paismart.pdf       1
4 2025-07-14 03:21:38.008598 57fa35b1f54af2ac1ff801a19a503143 。 简历0704.docx          1

刘        chunk_index chunk_md5
HBc varchar(255)

## bgint       着训
2e8d7e452961fa303fe495756fa82174
a964e7d817bd76710952d81e604e1067
人3ba82ec5664767e2c487141fa08a03

57fa35b1f54af2ac1ff801a19a503143

当所有分片上传完成后，前端会调用后端的合并接口。这里我们用的是 MinlO 提供
内存和 CPU 资源。合并

Paismart

Explorer              Uuilsjava

Folder

btic ctass

的 composeObject功能，

系统会反文件状态在 MySQL 里更新为已完成'，并且清理掉对应的分片文件和 Redis 记录

file_md5

ec varchar(255)
2e8d7e452961fa303fe495756fa82174
a964e7d817bd767f0952d81e604e1067
亿3ba82ec5664767e2c487141fa08a03
57fa35b1f54af2ac1ff801a19a503143

storage_path
pc varchar(255)

chunks12e8d7e452961fa303fe4
chunksla964e7d817bd76710952
chunks/fa3ba82ec5664767e2c4
chunks157fa35b1f54af2ac1ff801

直接在存储端完成分片的合并，完全不占用服务器的

ejava x 。 马 HybridSearchSservice

main                                    private            ChunkInfof         fitehd5，       chunkIndex          chunkhd5





## 第 2 页


FileTypevalidations

paran
paran
return

Tokencacheservic

ER
@    Outline                                  pubtic String             (CString          String

Language SupportforJava(TMJ) 1。 5             String          =            (        )
ogger                                                  4

一                                tr
加 fileUploadRepository                 |

加 chunkinfoRepository
加
uploadchunklstingi
@@ getFilerypelString)
CD schunkuploadedfstr
CD markchunkuploaded
CD daletefieMarktsting)

O

OO isBitset(byte, int)
OO gerrotalchunkststing
@ savechunknfotsting，
mergechunkslstig,
Timeine

Mogger，
List<ChunkInfo           chunkInfoRepository
Togger，                                              d

评
Togger，

throw new                 (string

Java Projects
List<String:

文本切片、向量化等工作，保证上传接|

LoguUtits .               peration:"MERGE_FILE"，
kafkaConfig .                       (0)，

kafkaTempLate .       kafkaConfig .

LogUtitLs .             )peration:"MERGE_FILE"，

2.分片上传…那你是如何知道哪个分片属于哪个文件的?

5 算法计算出该文件内容的      值，也就是 ileM
inkindex，用于记录当前分片





## 第 3 页


Q@param
Q@return
Qthrows IO0Exception

= "chunks/" +      + /+
1Logger .    (format: "构建分片存储路径 => fiLeName: {+，path: {"，

try 并

Logger .   (format: "开始上传分片到MinI0 => fiLeMd5: {+，fiLeName: 1

Put0bjectArgs                = Put0bjectArgs .
2: "UpLoads")

)

minioCcLient.                );
Logger .   (format:"分片上传到MinI0成功 => fiLeMd5: {，fiLeName: {

把所有分片拿出来，按 chunklnd

List<ComposeSource>         至
(     -> ComposeSource.
(CoLLectors .       ());

at : "构建合并请求 => fiLeMd5: {，fiLeName: {，tan
，        ，       );

minioCLient .                       (
Compose0bjectArgs .        OO)

(Cname:"upLoads")
(            )
《

3.如果上传中网络断了，如何实现'断点续传?

D，另一方面也会用 Redis 的 btmal





## 第 4 页


Q@param
Q@return

pubLic int                (String        ) {
1Logger .    format : "计算文件总分片数 => fiLeMd5: {}+"，      );
try 工
0ptionaL<FiLeUpLoad>              = fiLeUpLoadRepository .

片数 => fiLeMd5: {"，

= 5*1924 * 1924;
= (int) Math.     (doubtLe)

数计算结果 => fiLeMd5: {+，totaLSize:

Q@param
Q@param
Q@return

private booLean

try
int
int

庄

return faLse;

return           [
} catch (Exception e) {





## 第 5 页


1Logger .error(format:"检查bitmap位状态失败 => bitIndex: {}，错误: {j}"
1Lse;

Redis 刚好适合这种场景。它是内存型的键值对存储，读写速度特别快，而且我们用的是 Redis 的 Bitmap。简单来说，我们会用文件的 MD5 作:
后用一串"0和"1"的位图来记录每个分片的状态，比如第 0 个位代表第 0 个分片，第 1 个位代表第 1 个分片上传一个分片就把对应的 bit

这样记录状态特别省内存，例如，要跟踪一个 100 万个分片的文人
本就是 O(1) 的时间复杂度

5.这些上传的临时分片，存在哪?

存在 Minla 里
因为分片上传场景下，文件往往比较大，而且一旦上传中断或者失败，之前已经上传的分片是需要持久化的

需要大约 122KB 的内存 (1.000.000 bits /8 / 1024 = 122 KB) ，而且查询和更新都很块，基

Minlo 还是一个道循 53 协议的对象存储系统，天然适合这种大文件、多分片的场景，而且支持高并发读写，性能也不错

并且所有分片上传成功后，还需要一个合并操作，MinlO 恰好就提供了这么一个 API-composeObject

List<ComposeSource> Sources = partPaths    3站
p(path    ComposeSource.buiLder() .buc
LLect(CoLLectors.toList()

Logger .debug(format: "构建合并请求 => fiLeMd5: {，fiLeName: 寻
fiLeMd5，fiLeName，mergedPath，partPaths

"upLoads")
object(mergedPath)
(sources)
buiLd()

片合并成功 => fiLeMd5: {，fiLeName: {}，

6.详细描述分片上传与断点续传的实现机制。在这个过程中，Redis和MinlO分别承担了什么核心角色?

片上传，每个分片在上传成功之后，后端是直接把它存在 MinlO 里的。等所有分片都上传完成后，我们会调

Minlo 的 compose 接口，在服务端把这|

前端                                后端                              Minlo





## 第 6 页


再说一下断点续传

光有 Minlo 还不够，因为我们还需要知道当前这个文件上传到第几块了，哪些分片已经传过了。所以在上传的过程中，每当一个分片上传成功，后册全在 Redis 里
记录       上上传拓相

un 长1丰字有

bu 到本的尖下手世有8个b，短个b用0吕者1和示

un                         anml | ，        日        1        日        1         1        日        1

un = nar，代于更

具体实现上，我们是用 Redis 的 Bitmap 来做的，把文件的 MD5 值作为 Redis 的 Key，每个分片对应 Bitmap 里的一个 bit 位，上传成功就把那个 bit 设置成 1。

这样后册只需要再给前册提供一个查询分片状态的接口: 告诉前册哪些分片已经上传了，哪些还没上传，这样前端就可以进行断点续传了。后喘也会在合并前做一
个完整性校验，看是否所有分片都到齐了。

总结来说，MinlO 主要负责存实际的分片数据和最终的完整文件，Redis 主要负责存上传过程中的状态。

7.如何处理上传过程中的各种异常情况? 例如，如果用户的网络突然中断，或者某个分片上传失败了，你设
计了什么样的恢复机制?

假如说用户的网络突然疡了，或者浏览器则了，派卫明的断点续传机制仍然能够保证用户重新将自己的文件上传成功。

人 了

单线程        昌

算传输的起始位置
自分片或者下分

人 区取|

包 计算传输的起好

多线程

加 分片后并发传输

Q 结襄传输通知

追加临时文件，结来时更改为正式的文件扩展名

返回断点位置，即当前文件字节数

创娃分片文件

断点续传

os有。 改攻| ismests sas大六-
整合文件，如果为最后一个分片时，合并分片
创建分片文件

2 国国| ar    运回最大分块-1
整合文|  收到客户详上传结束标

首先，每个成功上传的分片状态都已经记录在了 Redis 的位图当中，因此上传进度不会因为用户网络的中断而委失。

其次，针对单个分片的上传失败，比如说因为网络拉动或者服务端临时出错导致的上传失败，前端是有内置重试机制的。也就是说，如果某个分片上传失败了，前
端不会直接玫弃，而是会自动重试几次，当然了，我们设置了最多三次，每次间隔逐渐拉长。





## 第 7 页


bootLean fiLeExists = fiLeUpLoadRepository.findByF       dUserId(fiLeMd5，userI(
ger .debug(format : "检查文件记录是否存在 => fiLeMd5: {+，fiLeName: {+，fiLeType: {

(!fiLeExists) {
Logger .info     "创建新的文件记录 => fiLeNd5: {，fiLeName: {f，fiteType:
fitLeMd5，fiLeName，fiLeType，totatLSize，userId，orgTag，isPubtic

FiLeUpLoad fiLeUpLoad =    FiLeUpLoad
fiLeUpLoad     LeMd5(fiLeMd5
fiLeUpLoad

fiLeUpLoad

fiteUp1toad

而且我们|

口做了畦等设计，哪怕同一个分片被重复上传多次，后

别出来，不会存重复数据或者状态混乱

(!chunkUpLoaded) {

Logger .debug(format   算分片N05 => fiLeMd5: {+，fiLeName: {，chunkIndex:

byte[] fiLeBytes = fiLe.getBytes

chunkMd5 = DigestUtiLs.md5Hex(fiLeBytes

1Logger .debug(format:"分片MN05计算完成 => fiLeMd5: {+，fiLeName: {，chunkInde|
fiLeMd5，fiLeName，chunkIndex，chunkMd5

StoragePath = "chunks/"” + fiLeMd5 + "/" + chunkIndex
Logger .debug(format: "构建分片存储路径   fiLeName: {+，path: {"，fiLeName

1Logger .info(f   t: "开始上传分片到MinI0 => fiLeMd5: {，fiLeName: {，fi
fiLeMd5，fiLeName，fiLeType，chunkIndex，storagePath，fitLe.9

Put0bjectArgs put0bjectArgs = Put0bjectArgs.buiLder()
ucket(name:"upLoads")

ject(storagePath)

片上传的时候，前负会持这个分片的 MD5 什
翻转) ，我们就会直接拒绝这个分片，告

块发给后端。后端在收
凋这个分片坏了，请重传~，保证最终存下来的数据是没问题

再有一点，我们对数据的完整性也比
D5，对比一下，如果发现数据被破坏了《比如有上

务端自身的问题，比如说 Minlo 挂了，或者 Redis 短暂不可用，我们目前的做法是让前册去儿底。服务喘会把错误返|
等服务恢复后用户这过     续上传，不用重新

8.文件合并过程中，如何保证原子性? 如果合并过程中失败了，你有什么回滚机制?

在我们的项目中，文件合并涉及应用服务器、MinlO 对象存储、数据库和 Redis 缓存的协同，所以它本质上是一个分布式操作

给前端，前册那边的重试机制会自

关于这个问题，我分析了我们的核心代码 Uploadservice. java 中的 wergechunks 方法，可以将它分为两层来看

我们项目中，最关键的合并步邓是调用了 MinlO 的 conposeobject 接口。这个操作是在 Minlo 服务册内
Minlo 来说，分片合并要么就彻度成功，生成一个完整的最终文件; 要么就失败，不产生任何文件。不会
基础的保

整体业务流程的一致性与挑战

成的，它本身是一个原子操作。这意味着，对于
一个"合并了一半"的损坏文件。这一点为我们的数据

但是，整个业务流程 (nergechunks 方法) 包含了多个步骤: 1. 调用 MinlO 合并 -> 2. 清理 Minlo 中的分片 -> 3. 删除 Redis 缓存 -> 4. 更新数据库状态为"已完




## 第 8 页


这个体流程，在当前的实现中             ，并且|

这是最简单的情况。 Minio 没有生成新文件，代码会抽出异常并终止。此时，分片、数据库记录、Redis 缓存都
并即可。这种情况风险较小

保留在合并之前的状态，用户只需要重新扔发合

比如，文件在 Minlo 上已经合并成功了，但在更新数据库状态时，数据库突然罕机或网络中断。这会导致系统进入一个|       的状

合并中"，但永

无法自动修复这个错误状态

针对这个潜在的数据不一致问题，已经有了解决方案，我会引入             的思想，来保证系统的;

具体来说，我会分两步走;

D、                 首先，我会在数据库的文件上传记录表中增加一个中间状
在调用 MinlO 的合并接口之前，就先把状态置为:

，比如      代表“合并中-。整个洒

从

个定时的后台任务 (例如每小时扫

一次) ，这个任务专门做"多底和-校准-的工作。

。 它会扫描数据库里所有状态为合并中并且"卡住'超过    间 (比如 1 小时) 的记录
，。 对于每一条记录，它会去 Minlo 查        终文件是否存在

如果:     ，说明当时只是后续步台失败了。校准任务就会主动完成条下的工作: 将数据库状态更新为     ，并清理分片和 Redis 缓存.
。 如果:      ，说明当时是核心合并步对就失败了。校准任务就会把数据库状态重置为-上传中-，以便用户可以重新尝试

可以用来清理那些因为各种异常而残秆的珀儿分片数据，避免存人

通过这种方式，即使在执行

得中发生任何单点故障，系统也有具备了|                                  的能力，从而大大提升了文件上传功能

先说所有权的旭                                   如分片上传、合并操作，前端都必须带上 token。这个 token 在后喘被拦夫器解析之
后，会提取出当前有                             把这个 userid 和文件的 MD5、文件名等信息一起写进数据库，保存到 fle_upload 表里.
这个过程其实

fiLeUpLoad = new FiLeUpLoad()
fiLeUpLoad.setFiLeMd5(fiLeMd5)
fiLeUpLoad.setFiLeName(fiLeName)
fiLeUpLoad.setTotaLSize(totaLSize)
fiLeUpLoad.setStatus(status:0)
fiLeUpLoad.setUserId(userId)
fiLeUpLoad.set0rgTag(orgTag)
fiLeUpLoad.setPubLic(isPubLic)
try {

fiLeUpLoadRepository.save(fiLeUpLoad
info(f

接下来，比如合并文件这种敏感操作，系统会再做一次权限校验。我们会通过 fleMd5 + userld 去数据库查这个文件记录，确认这个操作
防止用户去合并别人的文件。

是这个用户发起的，

然后是访问权限这一块，我们用了一种叫做组织标签的机制。简单理解就是
见-或者"仅自己可见-。如果前绸不传这个字段，我们会给它加上一个默认的组织标签-就

用户可以指定这个文件是
这个用户的主组织，避免权限空白

等"下可见的。比如说-研发部可

if (orgTag         11 orgTag.isEmpty()) {
try {

TogBusiness((   t            UserId，n
primary0rg = UserService.getUserPrimary0rg(userId





## 第 9 页


这些权限标签会一起保存到 fle_upload 表里，下游的检索服务也会根据组织标签去做权限过滤，保证-有权限的人才能看到对应的文件。

10 .为什么最终选择了Kafka来实现异步处理? 相比于其他消息队列 (如RabbitMQ) ，它有什么优势?

第一，比如说我们在文件上传完成之后，会有很多后续的异步任务，比如提取元数据、生成全文索引、做 Al 向量化之类的处理
本身也比较消耗资源，属于典型的流式数据处理场景-

处理不仅数据量大，而且任务

Kafka 本身就非常适合这种高委吐的数据流。它的架构决定了它可以处理每秒上百万条消息，而且是基于磁盘顺序写入，非常稳定，这跟 RabbitMQ 那种内存优
先、消息读完即列的机制不太一样

另外一个很关键的点是 Kafka 的数据持久化能力。它的消息是保存在磁盘上的，而且可以保留很人，比如 7 天。消费者通过 offset 去控制消费进度。这个机制带来
了一个特别大的好处: 我们可以随时进行数据回源。

Kafka Log Implementation

Segment Fles
topicna477a4gg6a ka
Actve segment st。 agoaTe
77949968 -359551592051               [Te

51592052 -36625553894
6265603605 -97669079630
0009079651 360772817944

[ETEEIEICI

essags 35551592051

48748861 722490796

1722490797 82796232651
2796232652- 838669746531         一

ER
essaga 394677850175

[EEC
essage 35551592051

比如某个向量化模型升级了，或者数据处理远辑修复了 bug，我们只需要新建一个消费组从历史 offset 开始消费，就能批量重跑所有老的文件数据，不用动数据

Kafka 现在的生态越来越强，比如 Kafka Connect、kafka Streams、ksqlDB 这些都很成热了。昌然现在我们主要是用它做异步任务解揭，但未来
计算、流式分析，其实也可以直接在 Kafka 的基础上扩展。

11.在使用Kafka时，你是如何配置来确保消息的可靠传递的?

首先是生产者这块，我们设置了 acks=all，也就是生产者发送
靠性设置，哪伯 leader 写完之后宪机了，只要follower 写成功了

须等到所有同步副本都写成功，才算这条消息真正发出。这是 Kafka 提供的最高级别可
落盘，过免了"刚写完就丢"的风险-

就不影





## 第 10 页


pubtic ProducerFactory<String，0bject>
Map<String，0bj       new HashMap<>();

config.put(Prod  rConfig.B00TSTRAP_SERVERS_CONFI6，bootstrapSei    )
config.put(ProducerConfig.NEY_SERIALIZER_CL4SS_CONFI6，StringSeriatizer.cLass);
config.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFI6，JsonSeriaLizer.cLass) 1

return new DefauttKafkaProducerFactory<>(config);

@B
pubtLic KafkaTempLate<String，0bject>

return new KafkaTempLate<>(producenrl

@B
pubtic ConsumerF    y<String，0bject>

Map<String，0bject> config = new HashMap<>()i

config.put(ConsumerConfig.B00TSTRhAP_SERVERS_CONFI6，bootstrapServers);
config.put(ConsumerConfig.6GROUP_ID_CONFI6，fiLeProcessing6roupId);
config.put(ConsumerConfig.KEY_DESERIALIZER_CI   CONFI6，StringDeseriatLizer.cLass)]i
config.put(ConsumerConfig               CONFI6，JsonDeseriatizer.clLass);
config     s                   PACKAGES，trustedPackages);

return new DefauLtKafkaConsumerFactory<>(config);

， 我们把 topic 的副本数设成了 3，也就是一个leader + 两个follower，再加上配置了             ，意思是如果只有一个副本存活了《比如
只有 leader 没挂) ，Broker 就会拒绝写入请求。这个机制相当于在服务端也加了一道"健康检查*，防止我们写入到一个马上就挂掉的分区上，吉免数据丢失。

，我们没有用 Kafka 默认的自动提交，而是设置了                转为手动提交。这样能确保只有当我们的业务远辑真正执行成功，
比如向量写入 ES 成功，数据库状态更新成功，才会调用        提交 offset。这样即使系统莉渍，重启后还能从失败点重新拉取任务，保证至少处理一次,
不会委任务.

生产者在发送消息到 kafka Broker 的过程中，可能会因为网络拉动、Broker 临时故障等原因失败。为了应对这种情况，Kafka 生产者内置了简单的重试机制。在派
陪明项目中，我们为 kafka 的 retries 参数设置了 3

kafka :

enabled:

bootstrap-Sservers:

producer :
key-seriaLizer:
VaLue-seriaLizer:
acks:
retries: 3

enabLe-idempotence :

，当生产者发送消息失败时，它会自动岩试重新发送，最多 3 次。同时，                的配置也能确保即使在重试过程中，消息
过免了因重试导致的数据重复问题

消费者的重试比生产者稍微复杂一些，因为它处理的是业务远辑的失败。比如，
模型芹时不可用等原

派脱明中，消费者拿到文件处理任务后，可能因为数据库连接超时、embedding
直理失败。如果此时直接确认消息，这条任务就丢失了; 如果不确认，消息会一直被重复消费.

派脱明通过 DefaulErrorHandler 实现了一套优雅的消费者重斌
败，它会再等 1 秒，进行第二，

项目中是 ile-processing-dht)

列机制。FixedBackOff 会阻室当前消费线程 ，等待 1和      重试。如果再次失
重试，总共最多重试 4次。如果 4 次后仍然失败， DeadLetterPublishingRecoverer 会接管这个消息，将它发送到死信队列中(在

        > KafkaListenerContainerFactory(

consumerFactory，




## 第 11 页


DeadLetterPubtishingREEGUERET           = new

-> new                fiteprocessingDttTopic，

DefauttErrorHandter         = new            3666L，4));

ConcurrentkafkaListenerContainerFactory<String，0bject>
(               );
(            9
return

全这程你是如何直理不同格式(PDF、Werd、bx) 的文档的? 到的最可的技术玲点是
什么1“

我们采用了开源工具 Apache Tika 来完成文档的解析工作。其核心组件 Au      rser 能够自动识别上传文           Word、tbt等)，并
本内容.

String extractText(InputStream fiLeStream)      I0Exception，TikaExcept
{

ByteArray0utputStream baos =    ByteArray0utputStream() ;
fiLeStream.transferTo(baos) ;
InputStream cachedStream =    ByteArrayInputStream(baos.toByteArray());

BodyContentHandLer handLer =    BodyContentHandLer(        -1);
Metadata metadata =    Metadata() 1;

ParseContext context =    ParseContext();

AutoDetectParser parser =    AutoDetectParser();

parser.parse(cachedStream，handLer，metadata，context) ;

1ogger.debug(        ) ;
(String name : metadata.names()) {
Logger.debug(      ，name，metadata.get(name));

String content = handLer.toString() ;
1oggen.debug(            ，content) ;

(content .isEmpty()) {
1ogger.warn(

content;





## 第 12 页


private List<String>
List<String>

String[]
StringBuiLder
for (String

于

List<String>

这样，embeddi

15.当处理一个超大文件 (>10G1

为了解决超大文件的      采用了流式处理，尽可能在任何时候都只处理一小段数据，吉免全量加载
String extractText(InputStream fiteStream)      IOException，TikaException {
checkhemoryThreshotd();
(BufferedInputStream bufferedStream = new BufferedInputStream(fiLeStream，bufferSize])

StreamingContentHandLer handLer = new StreamingContentHandter();
ECSOECSTTETSOSCITE TEWTTETS0STSUJ7
ParseContext context = new ParseContext(;

AutoDetectParser parser = new AutoDetectParser();

hecknemoryThreshotd(O) 1





## 第 13 页


Long maxMemory = runtime.maxMemory();
Long totatLMemory = runtime.totatMemory()
Long freeMemory = runtime.    Memory() ;

Long usedMemory = totatMemory - freeMemory;

doubLe memoryUsage = (doubLe) usedMemory / maxMemory;

if (memoryUsage > maxMemoryThreshoLd) {

1ogger.warn(     E                             ，memoryUsage * 199);

System.gc0O;

UsedMemory = runtime.totaLMemory() - runtime.freeMemory();
memoryUsage = (doubLe) usedMemory / maxMemory;

if (memoryUsage > maxMemoryThreshotLd) {
throw new RuntimeException(
String.format(      ，memoryUsage *   ));

豆包embedding AP| 是整个 RAG 流程中非常关键的一环，主要负责文本的向量化

针对豆包 embedding AP| 有时候会出现的网络拉动或者服务自身压力过大的情况。我们在 EmbeddingClient 中接入了一个简单的重试机制，基于 WebClient 的
retryWhen 操作符。具体来说，只要是 WebClientResponseException 类的错误，我们就自动重试 3 次，每次间隔 1 秒。这个方案在面对短暂性的    站果很
好

private String         (List<String> batch) {
Map<String，0bject> requestBody = new HashMap<>();
requestBody .put(     ，modeLId);
requestBody.put(     ，ba
requestBody .put(

return webCLient.post()
.Uri(                )
.bodyVaLue(requestBody)

bodyToMono(String.cLass)

.retryWhen(Retry .FixedDeLay(         ，Duration.ofSeconds(1))
fitLter(       e -> e instanceof WebCLientResponseException))

.bLock(Duration.ofSeconds(36));

对于每次的 AP| 请求，我们设置了一个 30 种的超时时间。这个设置非常重要，因为如果不做限制，万一外部服务响应很慢，我们的线程可能会一直阴塞在那儿，
不仅影响用户体验，还可能拖幸整个服务

就是频率控制。虽然我们没有显式使用 Guava 的 Ratelimiter 做限流，但是

首先，ES 并不只是一个简单的全文搜索引擎，它已经内置了向量检索的能力。而且 ES 特别适合做混合搜索，像我们现在的需求，是妍要支持语义相似度这种
搜索，又要支持关键词的精准匹配。这些能力，ES 都能一体化搞定。具体实现上，我人用了 knn 做初步召回，再通过 rescore 用 BM25 做精排，效果上也能兼顾相
关性和语义相似度

你解决了，比如服务注册、集群分片、副本、高可用、监控等等，我们不

如果换成 FAISS，开发成本会急剧上升。但下一个版本，我们是打算升级到 FAISS 的




## 第 14 页


用户在上传文件的时候，我们会先把文件存到 Minlo，然后给 Kafka 的 fle-processing 主题发一条消息。这个消息里会带上文件路径、MD5、
息。这样前册请求就能秒回，后续的重任务就交给后台慢慢处理.

Kafka 的消费者需要做三件事
第一件是从 MinlO 下载这个文件，拿到文件的输入流;

第二件是解析，我们会用 Apache Tika 把文件内容解析成文本，然后做分块，再把每个文本块存进 MySQL。这里有一个关键点: 每次上传的文件，分片数据会先落
座MysQL

第三件是做向量化和存入 ES。它从 MySQL 把刚存进去的文本块拉出来，调用外部大模型 (比如豆包 embedding) 生成向量，然后通过再一口气写入 ES。这时候
我们才算真正完成了文档的向量化和可检索化

要保证最终的一致性，需要

首先是重试机制。对于 Minlo 下载失败、向量服务超时、ES 写入异常这
耗时不超过5 秒

景，我们可以用 Spring Retry 的      的注解，设定 3 次重试、每次间隔 1 秒,

大重试次数后，把，

其次是死信队列。我们可以在 Kafka 的配置里加个 dead-letter 主题，比如 fle-processing DLQ，当超过|
苹    任务来分析 DLQ 的原因，避免长期

最后是任务状态追踪。我们在 MySQL 建一张表，每个文件处理一次就记录一条，标记它当前的处理状态，比如 PENDING - PARSING - VECTORIZING 一

CoMPLETED 或者 FAILED。 这样我人

可以清楚知道哪些文档同步失败了，基至

持后台补偿重跑
kafak 生产端

max-requesSt-S
kafka:
enabted: true
bootstrap-servers: 127.9.6.1:9992
producer
key-seriatizer: org,.apache,kafka.common.seriatization,StringSeriatizer
vatue-seriatizer: org.springframework,kafka.support.seriatizer.JsonSeriatizer
acks: attL
retries
enabte-idempotence: true
transactionat-id-prefix: fite-uptoad-tx-
properties
ctient.dns.tookup: use_atL_dns_ips

<String，0bject>
<String，0bject> config = n       <>();
config.put(ProducerConfig,BOOTSTRAP_SERVERS_CONFIG，bootstrapServers);
config.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG，     StringSeriatizer ctass)有
config.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG，     Jsonseriatizer.ctass)

config.put(ProducerConfig,ACKS_CONFIG,      "att)
config.put(ProducerConfig,ENABLE_IDEMPOTENCE_CONFIG，      true);
config.put(ProducerConfig.RETRIES_CONFIG，       )

DefauttkafkaproducerFactory<String，0bject> factory = new

factory.                                     "fite-uptoad-tx-，
return factory;

已实现 【生产册可靠投递) 核心逻辑，新夫

、applicationyml/ application-devyml

。在                 节点新增

了、kafkaconfig

。 producerfactory 里同步

。 创建                 后                          开启事务能力
、UploadControlle

        方法加          保证 MySQL 更新与 kafka 发送|

。 使用                       __) 发送消息，确保与生

作用

MySQL            更新成功 一 Kafka 消息一定写入; 若发送失败事务整体回滚，避免数据不一致

送喘启用守等 + 重试，确保

时消息至多写入一次




## 第 15 页


,系统级别买现"写库 + 发消息"原子性，为后续消费端重试/死信也合提供可午前皖。

afka 消费端

consumerFactory，

> kafkaTemplal

kafkaTenplatey

record，ex                              ，recordl

consumerFactory)

修改后的消费端在可售性与可观测性上获得了显著提升，主要体现在以下 5 个方面

、自动

试机制

pefaultErrorkandler * FixedBackoff(3s， 人

。 每条消息在业务拉异常后会等待 3 秒再重放，最多重试 4 次，连同第一次共 5 次机会
。 效果: 解决明时故障 【网络拌动、外部接口超时等) 导致的-偶发失败

也、死信队列 (DLT)

。 beaatetterpublishinghecoverer 椅重试仍失败的消息转发到 file -processing-dlt 主是
。 分区号保持与原消息一致，方便按源分区并行补偿-
。 效果: 把-持续失败"的及数据与正常流量隔离，防止阻塞主消费; 同时为后续人工/自动补偿提供集中入口

可、至少一次投递保证

。 业务方法不再自行 *ry/cstch，而是直
。 spring-kafka 仅在处理成功后提交 offset，失败则不提交 -消息一定会重新消费或进入 DLT
。 效果: 避免"处理失败但 offset 已提交"导致的数据委失<

加、配置驱动的 DLT 主题

evalue(rstspring_kafka topic-dltj") 注入 filepi      ic， 扬脱硬编码
。 可在不同环境通过yml 轻松指定 DLT 名称、分区数、保留策略

。 效果: 运维、变更更加灵活统一。

号、代玛职责更清晰

。 业务代码 (FleprocessingcConsumer) 只关心核心到辑，不写重试/告警/投递称加
。 错误处理征咯集中到 KafkaConfig，符合-横切关注点分离原则
。 效果: 降低业务代码复杂度，可单独调优重试和 DIT 策略.

整体收益:

。 对用户接口: 上传后的后台处理不再"悄悄失败"; 失败任务要么被自动恢复，要么沉淀到 DLT
。 对运维: 可监控 DLT 累积量触发告警，并通过单独的 Listener 或脚本批量重放
。 对系统: 实现"至少一次 + 可补偿的可靠消费链路，为跨 My5QL/ Minlo / ES 的最终一至性美定基础

19. 当需要删除一个文件时，如何确保跨多个存储系统的一致性删除? (待完善)

当前代码中"伍除文件"走的是同步流程，直接在 Documentservice deleteDocument 内按顺序调用各数据源，未使用 Kafka

。 Elasticsearchservice deletepyFileMd5 -- 删除向量索引
。 MinloremoveObject ~ 副除对象存储中的合并文件
。 DocumentvectorRepository ~ 副除MySQL document vectors 记录

。 FieuploadRepository ~ 副除MySQLfle upload 记录

整个方法被 @Transactional 包囊; 若中途挑异常会回滚数据库层面的操作，但不会触发异步消息，也就没有 Kafka 介入，

只有"上传合并完后触发后台解析/|

量化-的场景才用 Kafka; 文件删除暂时是不经消息队列的。

@Transactional 只能保证 受同一事务管理器管辖的资源 (本项目是 Spring JPA -MySQL) 的原子提交与回滚; MinlO、Elasticsearch 等外部系统并不会加入到这段
数据库事务里，因而存在两类不一致风险:

了、数据库回滚无法-投销"外部动作

。 代玛先油 ES / MinlO，再赋 MySQL 并提交事务
。 着提交阶段 MySQL 失败并能发回液，ES、MinlO 的便除已生效 - 数据丢失与元数据仍在的错位

也、外部删除失败导臻数据库回滚，但外部已部分成功

。 先册 E 成功 - 接着一 MiniO 抛异常 - 方法撼 RuntimeException 一 JPA 回滚;
。 结果: ES 已测，Minio / MySQL 仍在，同样不一致

要真正保障路存储一致性，一般有三种思路




## 第 16 页


回、分布式事务 / XA (理论可行，实践代价高)
MinlO、Elasticsearch 并不天然支持 XA，两段式提交在云原生场景也不推荐。

回、事件驱动 + 最终一臻《常用做法)

。 只在事务内修改孝据库并写一条"扣除事件到 Kafka (用 Outbox / 事务消息保证原子) 。
。 独立消费者监听事件，再调用 ES、Minlo 扣除; 家等设计 + 重试/ DLT 可收全到最终一至

国、补偿/ 对账
若坚持同步删除:

。 将而数据库"放最后，且捕获外部三除异常记录待补偿;
。 每史对账: 若 DB 无记录但 ES/MiniO 仍有文件，则补副; 反之则补回 DB。

综上:

@Transactional 只能保证 MySQL 层的一致性; 若要跨 MySQL、MinlO、Elasticsearch 保证一致，需要引入"事件 + 补偿"机制或专用的分布式事务框架，单靠注解
无法做到。

我们把国除操作拆成了两个阶段。第一阶段是软铀除，也就是用户发起谢除请求后，我们只是在数据库中把这条文件的状态标记为 DELETING，并且记录下
fleMd5、userld 等关键元数据-

文件列表  十W当|] C 居所 窗列于

人 克认除当前文件吗?

文件名                                                       文件大小上传状态 。。 组织标签               是否公开 。。 上伟              而
得   wangertbdt                                       OogK           已完成        管理员组织             型有|         2025-06-30 【mi除]
paismart pdf                                       os3k           已完成        束认组织               和有|          2025-07-02 【il除]
简历0704.docx                               1.44M         已完成       itwanger的私人空间 。 私有         2025-07-14  [mms]
沉于王二-稿件审核.docx                           97oM           已完成        管理员组织              私有|          2025-07-24   (mm]

等状态更新完后，我们会往 Kafka 发一条 file-deletion 的事件消息。这个事件就是我们删除操作的"触发器"，它由多个独立的消费者去异步处理:

。 有一个消费者专门处理 MiniO 文件讽除;
， 一个负责从 Elasticsearch 副除向量数据;
。 还有一个消费者负责最终的数据库清理《比如把 document_vectors 和 fle_uploads 表中的记录标记为 DELETED)。

每个消费者我们都加上因等处理。比如 MinlO 文件已经被删了，再收到消息也不会报错; 如果 Minlo 出现临时间题，还能通过 Spring Retry 做自动重试，最多重试
3 次。同时我们也配了 Kafka 的死信队列，一旦某条消息重试失败，系统会自动把它转到 DLQ，方便我们做告警和人工处理。

一        29人已点和          一

多国朋工序人@B人

图                                                                                                0 大带
热门评论 世

e@，                                                                    2026和02有26B 1t10

@派脱明 如果是大文件的话，分片数量应该很多吧，要写入的bi数也会很多，会不会超过bitmap的限制

出总革”国四

CT mm                                                                                                                              026征o2朋26B 1t1
针对大文件分片上传时Redis Bitmap的内存限制问题，结合参考资料分析如下:

核心结论
不会超过Bitmap限制，原因各下:
1 内在的宣志问




## 第 17 页


。 Bitmap采用位压缩存储: 每个分片公敌1 bit 【01状态) 。

。 例了0: 1GB文件按5MB分片约200个分片) 仅消耗 200 bits = 25字节;
10GB文件 (2000个分片) 仅消硅2000 bits = 250字和节.

。 即使100GB文件 (20000个分片) 也仅融 20000 bits = 2.44KB，运低于Redis音key限制《通常512MB)。
2 Redis Bitmap的容量上限
。 单个Bitmap支持 2432个bit (的40亿分片)，对应文件大小:
， 着按5MB分片: 最大支持文件 = SMB x 40亿 = 20PB;
 。 即使最小1KB分片: 最大支持文件 = 4TB。
。 此容量运超实际业务需求《通常文件不超过TB级)。
3 分片数量与文件大小的线性关系
。 分片数 = 文件大小/ 分片大小 (jp5MB)。
。 内存清耗与分片数成正比，但增长要其缓慢:
TB文件 (按5MB分片: 20万分片 = 24 4KB;
10TB文件: 200万分片 = 244KB.

实际场中的优化
， 乡片大小可调
可根据文件动态调整分片大小 (如大文件用10MB分片)，进一步降低分上数量。

， 焉立key管理
每个文件以 :ioya5 为独立Key，性免多文人数据冲突，有内存可回收 《合并后攻除Key)。

， Redis集群赤持
若并发上传文件极多，可通过Redi集群机向扩展，分数存鱼不同文件的Bitmap。

极端情况处理
车文件大到接近40亿分片 理论极限) ，可采取:

二 增大分片大小《如100MB分片) 将分片数夺纺至百万级
了 分片分组存储: 按文件分片范围拆分为多个Bitmap (如每1000万分片一个Key)。

总结
当前方案下，大文件分片上传的Redis Bitmap内存消耗可忽上略不计，运低于Redis音key限制和实际硬件内存容量，无需担心省出问题
出上划 国卓

1                                                                                                                                                                 026征0301日042
@泊有明 上传一个文件的时候如何判断是否需要调用查看上传状态的接口

针对大文件分片上传时Redis Bitmap的内存限制问题，结合参考资料分析如下;44 核忆结论 "不会超过Bitmap限制"“，原因如下; 1. “内存效率极高

册5 国上四

|                                                                                                                                                              12026年0有0日 042

在3瑞明系统中，通过文件上传杖态标志来判断是否需要调用查看上传状态的接口。系统会维护一个 ;suplcadiny 拟态变量，当读值为false时表示当前没有正在

进行的上传排作，此时可以安全地击发新的上传流程。上传开始前会先这个状态，确保不会同时进行多个上传任务。此外，在文件上传过程中，如果出现网络中电
或其他异常情况，系统会通过Redis记录已上传的分片状态，当网络恢复后，前端会查询这些状态以实现断点续传，这时也会调用查看上传状态的接口来确定哪些分
片已经成功上传。

@泊脱明 上传一个文件的时候如何判断是否需要调用查看上传志的接口
出 国昌

1                                                                                                                                                                oo6年有0 op46
uploading在哪里维护?@沽陪明

在浙脱明系统中，通过"文件上传态标志来判断是否需要调用查看上传态的接口。系统会维护一个|sUploading 状态变量，当该值为false时表示当前没有
出上划 明日

有                                                               026和0301日 047

在溪陪明系统中，isUploading状太主要在前炎组件中维护。根据参考资料[]和[10]，Ant Design的Upload组件会维护上传关态，包括Uploading、done和error等状
态.

具体来说:
1 前册组件通过"iii来归附上传坊，初给状态为uploading

 当上传完成后，状态会更新为done 或renor

3 在handleChange事件中，可以通过ren获取当前上传状态

4前端杖态管理(0React的usestatej可能维护一个sUploading标志，用于控制UI显示

在了明的实现中，后绸使用Redis的bitmap记录分片上传状态，但isUploading这个前坝志变量主要由前册组件自己维护，用于给用户提供实时上传反馈-
suploading在守里维护? 回泊联明

出5 用四

1                                                                026年3有01日0953
@泊聪明 请详细说明一下，从一个文件上传失败，到用户重新上传相同的文件再到调用查看这个文件状态信息的整个流程是什么

在沽脱明系统中，isUploading裤术主要在前册组件中维护。根据参考资料和[10]，Ant Design的Upload组件会维护上传坊，包括uploading、done和eror
页s 国昌

2                                                               oz6年有0 oas4




## 第 18 页


》

文件上传失败到重新上传及查看状态的完整流程
1 文件上传失败场景

当用户上传大文件时可能出现以下失败情况:

。 网络中断或浏览器天流

。 单个分片上传失败【网络动或服务负错误)

。 教据完整性校验失败〈分片MD匹配)

。 服务并临时故障 〈MinlO或Redis不可用)

2. 重新上传相同文件的流程

前端准备阶段

。 用户重新选择相同文件前端计算文件的MD5哈项值作为 less

。 将大文件切分为5MB的小分片，每个分片队各:ii eas 和 -hankraaex 《分片序号)

断点续机制

， 前训措名 ii eyes 请求后端查询已上传分片状态

。 后贿通过Redis的bitmap查询该文件的上传进度
， Redis key为 :lsyas ，value为位图表示各分片状态
。 1表示已上传，0表示未上传

。 前册根据返回状态，只上传未成功的分片，跌过已上传部分
后端处理流程
。 每个分片上传成功后:
工 存储到MinlO: 路径格式为 -hunke/ 231sya5111chankrndoxl
了 更新Redis捧术: 将对应分片的bi位设为1
3 首次上传时在MySQL记录文件元信息 《文件名、大小、上传者等)

合并阶段
。 所有分片上传完成后，前册调用合并接口
， 后响调用Minl的 -cspoascbjecr 在存储册合并分片
， 合并完成后更新My5QL 状态为已完成
。 清理Redis记录和分片文件
3. 查看文件状态信息
状态查询接口
， 前顺通过文件ID或 :il syas 查询文件杖态
， 后员从MySQL的 :ils_uplcaa 表获到信息:
。 文件基本信息 名称、大小、类型等)
， 上传关术【上传中/合并中/已完成)
， 权限信息 (所有者、""标签)

。 上传时间和进度

状态流转
上 - 计中2) -已
复制代码

异常处理

 。 后台有校准任务监控-合并中状态超时的文件
。 自动修复不一臻拓态，确保最终一至性

整个流程通过MD5标识文件、Redis记录进度、MySQL存全元数据，实现了高效可*的断点续传机制。

@派脱明 请详细说明一下，从一个文件上传失败，到用户重新上传相同的文件，再到调用查看这个文件状态信息的整个流程是什么

出1 国晶

10 条评论

二 的

引用原文: 尽管Tika 功能强大，但仍然存在不足

@派聪明 tika不能提取pdf文件蚂，项目里能做到提取叶些类型的文件
出 国卓1

Ti

根据参考资料中的措述，Apache Tika 在文档解析方面存在以下能力与局限性:

1 Tika 的核心能力

， 支持主流文档格式:
Ta 能自动识别并解析多种常见文件类型，包括:

office文档: Word ( .accx )、Exel ( xisx )、PPT ( -Fex )

， PDF: 普通文本型 PDF 《包含可提取的文本内容)
，缚文本文件 .xt 、 -av 等

2026和03朋MB ten1

026和0月14 1413




## 第 19 页


》

。 其他格式: HTML、Markdown、RTF 等
其核心组件 Aurcnerecezarsez 可自动识别文人类型并提取纯文本内容 (参考资料 [13]) 。

.Tika 的局限性
， 无法处理扫描件 PDF:

若 PDF 是扫描生成的图片型文件《无文本居) ，Tika 无法直接提取文本内容。这类文件需依吉 OCR (光学字符识唱) 技术《参考资料 [131)
解决方案规划: 项目计划集成 Tesseract OCR 引擎 来处理扫描件 PDF (参考资料 [13)

3项目实际支持的文件类型
结合资料中的实现远辑，项目当前可处理的文人类型包括:

文人类型                                                  冯持机本                                       技术方案
攻通文本型PDF                                           回支持                                        Ta直接提取文本
和和件PDF                                                X 析不支持                                    计划用TsseractOCR
wordreuceppr                                           加支持                                        Ta钊折
纯广本 (bd本)                                            加支持                                        Ta钊折
上jpg)                                        X 析不支持                               未在站各中提及
4 关键设计亮点
， 流式处理超大文件
通过自定义 scresningcanrearilandlex 边读取文件边解析，避免全量加载导致 OOM，适用于 >10GB 文件 (参考资料 [15])。
， 内存优化:
实时监控内存使用素，超过间值的发自动 GC 《参考资料[15])
总结

 ，Tika 能提取普通 PDF 的文本，但无法处理要提件 PDF.
， 项目当前支持: 普通 PDF、Office 文档、纯文本等
未来扩展: 计划集成 Tesseract OCR 以支持扫指件 PDF (参考资料[13])。

面上项 有加所
三 涨洒的故事                                                                                                 2026年03月14日 1406
引用原文: 这闫文档不外二可提取的本信息，下个上本我们打基集记Tesseract OCR 来记
tika不能提取pdf文件吗，项目里能做到提取哪些类型的文件
田B
人@ sm                                                                                                aeroavha ou

@派聪明 上传的文件存储的位置是minio，不是mysql。但第一伯是从 MinlO 下载这个文件，拿到文件的输入流;

第二件是解析，我们会用 Apache Tika 把文件内容解析成文本，然后做分块，再把每人文本块存进 MYSQL。这里有一个关键点: 每次上传的文件，分片数据会先落库
MySQL; 这里为什么是落库mysq

由ss 国旧1

                                                                                                                              12026和na月14B 1043
在文件上传流程中，原始文件分片确实存储在MinlO中，而MySQL存储的是两种不同类型的据:
1 文件元信息: 首次上传时，将文件名、大小、上传者、""标签等元数据存入MySQL，用于中踪上传拓态和权限控制。

2 文本分二: 文件上传完成后，系统会从MinlO下载完整文件，使用Apache Tika解析内容，然后解析后的文本进行分块处理，这些文本块最终存入MySQL 这
是为了支持后续的全文检索和文本分析功能。

这种设计是为了平街和存储效率和检索性能; MiniO适合存储大文件并提供高委吐量，而MYSQL存储文本分块便于快速检索和查询。原始文件分片不直接存入MySQL
足为了坦锡数据库存储压力过大，同时保证大文件存健的可"性.

出训 国四和

人@ sm                                                                                                assaata iom
上传的文件存储的位置是minio，不是mysql。但第一件是从 MinIO 下载这个文件，拿到文件的输入流;

第二件是解析，我们会用 Apache Tika 把文件内容解析成文本，然后做分块，再把每人文本块存进 MYSQL。这里有一个关键点; 每次上传的文件，分片数据会先落库
MySQL; 这里为什么是落库mysql

由ss共用四虽

加一                                                                                           oz6s03朋3621
 @滨肌明 项目为什么不选择RocketMQ而选择用Kafta昵，请结合项目具体分析
由sg 用

Ti                                                               oz6和0月1日1623
项目选择Kafka而非RocketMQ主要基于以下考量

.高时量与流式处理需求: 项目需要处理大文件上传[1G)及后续的文档解析、文本分块、向量化等流式处理任务。Kafka专为高委吐量场虹设计
数十万消息，特别适合这种大数据流处理场景-

2 数据持久化与回洲能力:Kafka消息持久化存储在科盘上，可保留较长时间(如7天)，支持数据回漳。当向量化模型升级或处理光辑修复时，可过过新建消费组从

，每秒可处理




## 第 20 页


3生态系统与扩展性:项目未来可能扩展至实时计算、流式分析等场景。Kafka拥有强大的生矶系统(Kafka Connecdt、Kafka Streams等)，可无继扩展，而

RocketMQ在这方面相对有限-

4 与ES集成优势: 项目使用Elasticsearch进行向量检索，Kafka与65有成娄的集成方案，可通过knn做初步召回，再通过rescore用BM25做稍排，

相似应-
5 运维简便性: Kafka架构相对简单，运维成熟，适合项目当前的技术枝和团队能力。而RocketMQ昌功能让富，但配置和运维相对复杂-
6 成本效益: 项目通过批量调用(每批100条)实现"变相限演"，这种第路在Kafka上实现更简单高效，降低了运维成本-

虽然RocketMQ在事务消息、延队列等特性上有优势，但项目当前场果更俩重高乔吐、持久化和未来扩展性，因此迁择了Kafka_

出上英国卓

引用原文: 用户只需要重新扣发合并即可

@泊和明 怎么卫新答发合并
页5半 国四1

Ta
根据参考资料描述，当文件合并失骸时，用户重新秀改合并的操作流程如下:

重新前发合并的具体步对

1前端发起请求
用户在前羔界面找到对应文件 (状态为-合并失败"或"上传路，点击"重新合并-按钮。前渤会乓带该文件的 人eMd5 向后端发起合并请求-

2 后册术验分片杖态
， 后央通过+:iieyss 查询 Redis Bitmap，确认所有分片是否已上传完成《所有 bit 位均为 1 )-

。若存在未上传的分片，系统会提示用户先完成利余分片的上传 避免无效合并)
3 执行合并损作
。 所有分片校验通过后，后毕调用 MiniO 的unposeabject 接口，在存储册将分片合并为完整文件 (不占用服务器资源)
。 合并成功后，系统更新 MYSQL 中的文件杖态为已完成"，并清理 Redis 分片记录和 MinlO 中的临时分片文件。
异步处理后续任务
合并完成后，系统通过 Kafka 发送消息，扼发后台服务执行文件解析、向量化等操作，确保用户快速获得响应。
关键设计保障

。 断点续传机制:
即使合并失败，已上传的分片状态仍保存在 Redis 中，用户无需重新上传分片，只震重试合并步对。

。 原子性保证:
Minio 的-=eceecrjecs 是原子措作，不会生成"半成品"文件，合并失败不会影响数据完整性。
天和处理:
后二对重复合并请求做需等术验，避免重复操作(如通过 :1eys5 “文件是否已存在)。

用户操作示例
假设用户上传了一个 1G8 的文件，合并时因网络中断失败:

1 用户在前冰界面看到文件杖态为合并失由
2 点击"重新合并"接钮 - 系统自动 Redis，确认所有分片忆上传-
3 后册调用 MinlO 合并文件 天态更新为"已完成
4 后台异步处理 (解析、向量化) 完成后，文件可正常使用。
加总结: 用户只坟在前点击一次"重新合并"，系统会自动完成利余流程，无和手动王预分片上传
出测 国昌

号 字第网

引用原文: Bimap

@派陪明
出总汪 国四1

Ti
在文件上传的疡点续传机制中，Redis Bitmap 用于高效管理分片上传杖态，具体作用如下:
核心机制
1状态记录
， 以攻伯内容的 MD5 值作为 Redis 的 key，每个分片对应 Bimap 中的一个bit 位 (0=未上传，1-已上传)。
。 例如: 文件被分为 1000 个分片，仅震约 122 KB 内存 【1000000 bits /181 1024) 妈避存储全部状态。
2 断点续传实现
。 上传中断后，前端携带文件 MD5 查询 Redis，获到已上传的分片位图。
， 前亲嘴过已上传的分片(bit= 1) ，仅重传未完成的部分 (bit-0) 避免重复上传>

技术优势
， 高性能: 读写操作时间复杂度为“O(I)"，适合高频分片状态更新。

， 内存高效: 相比数据库存储，Bitmap 大幅碱内存占用，遵免频紧磁盘 1O.
。 原子性: Redis 的位操作(如 seraz ) 是原子操作，确保杖态更新一至性

蒜顾相关性和语义

026和0月12昌2045

026征09月12日2046

12026年03月12日 2022

12026年03朋12日 2023




## 第 21 页


1分片上传: 每成功上传一个分片，后册执行 serarn fileas chunkzndex 1 。
2 关杰查询: 前册通过 =Tazw 获取分片状态，定位未上传的分片。
3 合并清理; 文件合并完成后，莉除对应的 Redis 记录和分片文件。
为什么不用数据库?
， 性能类颈: 频繁更新分片状态《如 1G8 文件分 200 片) 会导至数据库高并发写入压力-
。 资源兴费:临时状态数据无需持久化，Redis 内存存储更训效-
总续: Redis Bitmap 通过板简的二进制结构，实现了分片杖太的轻呈化、高并发管理，是疡点续传的核心技术支择.

册5册 国旧和

惫xxam                                                                              xzssoamnzB oa

引用原文: 让每个分块的前后都有一部分"上下文刀余

@派陪明，设置重生分块机制的目的是什么? 是为了后续文本向量化后好查询吗? 但是一个文件在前册分片上传完成后不是会将文件合并蚂? 文档向量化是对合并后的文件
进行向量化，那设置重你分块机制还有什么作用

四Ba

CT 5                                                                                                 026征0月12晶0845
您的问题涉及文档处理的核心浊辑，这里入要澄清两个关键概念 分片上传(传输层与文档分块(处理层》是独立流程。重全分块机制的作用如下:

二重生分块的核心目的: 解决语义断裂问题
问题背景:
若按固定大小切分文档(如每1000字符切一块) ，可能桂风一个语义单元(如包子、段落】强行拆开。例如

原文:人工知能的发展推动了"变革
分块1"人工智能的发展推动了

影响向量化后的检索准确性

。重夭机制的作用:
让相邹分块共享部分内容 (如前块未尾200字符 + 后块开头200字符) ，确保:
， 语义连续性: 即使分块在句子中间切断，重双部分仍保留上下文。

， 提升召回率:用户查询的关键洒若位于分块边界，重盘部分能三关该关键词，闻免沁检。

.为什么合并后仍需分块?
合并 vs 分块是两个独立步要:
。 分片上传: 将大文件拆分为小分片 (如5MB/块) 传输，合并后恢复为完整文件-

， 文档分块: 对合并后的完整件按语义重新切分 (如按段落/句子) ，用于向量化存储-
一 合并是物理拼接，分块是逻辑切到，二者不刘突.

， 向量化需要组粒度分块:
。 若将整个10GB文档作为一个向量，会导致:

。 向量维度过高，计算效率低;
， 无法定位具体内容，检索精度基。

。 分所后向量化: 每个分块生成独立向量，支持精准定位 (如 -第3章第2耻]。

3 重盘分块对向量化的具体价值

:                   1                       天和                               有和纪
了                 关多词在分块边界             可能沽检 (如 变革在块尾)                  通过重合部分一荔关键词，提升召回率
六基隆                上下六天，向量相人度低                  重要从义关联，提高计生
六                   化于果了                       全过人返回更充吾的内从片段
4 实际处理流程
an

用庆上博大文件】 -> [从具上传， sy /换】--。[富并文件] -， [文人纪;天人

> 量化| -> [让凶Es)

天名点:
， 步村< 《合并) : 仅居复文件物理完整性，不玫变后续处理这名.
， 纱村《分块): 对合并后的文件重新按语义分块，此时引入重航机抽-

总结
重和分块机制旺档向量化与检索优化的核心征路,，

1 避免语义断姓，提升向量质
村关边界关键词，提高检索召回率;

3 保持长文档的上下文连贯性，确保结果片段的完整性。
一 最终目标是让向量检索更精准、结果更符合用户语义需求-

与分片上传/文件合并无直接关系。其核心价值在于:

出5册 用四

人                                                                                                  xcsoana at

引

分片上传成

全尖了盯上对于”亿吕生忆mirir环昌te 加时此rinin 全旺豆的时三生辐从么|




## 第 22 页


由s呈日

Ta                                                               026征03朋1日 1050
根据参考资料，分片上传的写入顾序和异常处理机制如下:
工写入顺序
，先写 Minlo， 后写 Redis
每个分片上传时，后器会优先将分片数据存健到 MiniO (对铺丰储) ，存储成功后再用 Redis 的 Bitmap 记录该分片的上传太(参考资料第2、5、6部分)-
2.Redis 写入失败的后果

， 分片数据已存储，但状术委失
若 Minio 写入成功但 Redis 记录失败，会导致

。 分上文件在 Minl 中存在，但 Redis 中未标记该分片为已上传-
。 合并时系统误判法分片未上传，可能尖挤此分片 (参考资料第6部分)。

3 当前系统的应对机制
， 天等性校验
后端在接收分片时，会通过“iieyas 和 -huandex 校验分片唯一性。即使 Redis 记录失败，重复上传同一分片会被识别并拒绝《参考资料第7部分)。

， 合并前的完整性校验
合并前，系统会" Redis 中所有分片的状态。若发现革分片状态缺失《如 Redis 记录失败) ，会拒绝合并并提示用户重新上传(参考资料第6部分)。

4 改进方案 (规划中)

， 引入-合并中-拓态
在调用 MiniO 合并前，先符数据库杖态更新为合并中"( scaxas-> )。若后续步台失败 (如 Redis 记录中疡) ，后台任务会扫措 MinlO 文件是否存在，自动修复
状态《参考资科第8部分)。

后台校准任务
定时任务扫撕状态为“合并中"目超时的文件:

，。 车 Minlo 文件存在: 补全 Redis 记录，更新数据库为已完成
，。若 Minlo 文件不存在: 重置为"上传中"，多许用户重斌《参考资料第8部分).

总结
当前系统存在 MinlO 写入成功但 Redis 失败时状态不一致的风险，但通过 天等校验 和 合并前完整性”避免了数据损坏。"依赖 后台补偿机制 确保最终一致性，
避免用户重复上传。
而5 国B
图 mw                                                                                                    ossoanaoos

引用原文: 我是用 Redis 来管理分片上传的临时状态的。因为分片上传属于高频写入，比如一个 1GB 的大文件可能会被切间成上再个甚至上千个分片，每上传一个分
片，后出要记录一下"这个分片的状态"。如果是用 MySQL 的话，MYSQL 的压力会特别大，而且这些数据都是临时的，合并完之后就没用了，不值得进库。

这里是不是根本不对啊》? 其实也将每个分片的信息存入了MySQL，并且每次上传分片者查询MySQL，这个bitmap的意义何在? 3

南国上
四 mm                                                                    assoamneoass
@派陪明
面点革 加和
ash5847                                                                          2026年03月11日 2137

传入数据库是为了最后方便minio读取分片每一轮的上传者是走查询redis的(而且'edis快很多)只是写和数据库最后上传完毕才会读取数据库



