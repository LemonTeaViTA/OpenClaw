# 知识库检索设计方案

> 来源：知识库检索设计方案.pdf

> 解析时间：2026-03-14 16:29:36

> 本文档由 OCR 识别生成，可能存在少量识别错误


---


## 文档信息


- **总页数**: 8

- **文件大小**: 9.79 MB


---


## 第 1 页


14 文件上传解析设计方案

15 知识库检索设计方案

16 聊天助手设计方案

T7 派聪明库表设计

1 接口文档

星球

  派聪明 RAG知识库检索模块设计方案

知识库检索模块是派聪明这个 RAG 项目的核心功能模块我们是基于 Elasticsearch 实现的文档混合检索能力，将语义检索
和关键词检索结果结合起来，为用户提供更高质量的搜索休

该模块依赖于文件上传与解析模块完成的向量化处理，直接使用存储在 Elasticsearch 中的向量数据进行检索。系统目前使
用豆包 AP| 生成文本向量，并将向量存储在 Elasticsearch 中。

模块整体分为两大块:

D、知识库检索

，。 温全检索: 结合语义检索和关键词检索结果，按权重排序返回搜索结果
。 支持指定返回结果数量; 通过 topK 参数控制结果数量

习、权限控制

，。 基于组织标答的数据权限: 确保用户只能访问有权限的文档

。 默认标签全局可访问: DEFAULT 标和资源对所有用户开放

用到的技术村包括:

功能模块 技术选型 备注
全文检索 。 Elasticsearch 。 第一阶段，使用分词中

向量检索 。 Elasticsearch 。 第一阶段，使用dense vector类型

内面反考FA55       第二失笑，夫从更六性能的向量失守
级存    Reds     级存热点查询结果
孝据库 。。 MySQL    存储元数据
对象存储 。 Mini    存储文档文件
整体的流程是这样的:
Ce     [Ce       ae]     [Eee      aa ]

al

当用户发起一个查询请求时，系统首先会接收用户输入的查询文本，以及一些附带的检索参数，以及需要返回的结果数量
(topk)。在这一步，系统会先对这些参数做一轮合法性校验，确保格式正确、数据合理。

接着，系统会把用户的查询文本交给豆包提供的向量化 API，通过这个接口把自
量表示。这是我们后续进行语义匹配的基础。

语言的文本转换成可以用于向量检索的向

拿到查询向量后，系统会扫行一套混合检索流程，也就是结合语义匹配和关键词匹配-

在这一步，系统会构建一个 Elasticsearch 的查询语句，这个查询不仅包含了向量相似度的计算，还会结合全文搜索的匹配
结果。同时，我们还会在查询中加入权限相关的过滤条件，确保用户只能看到自己有权访问"的内容。

具体来说，权限控制主要分:

三条规则:

访问自己上传的文和
访问盾标记为公开的文簿，
的各限标签《比如部门或级季隐》，只要用户省有这些标答，也可以访问这此文挡-





## 第 2 页


带着这些权限条件，系统将完整的查询请求发送给 Elasticsearch，并基于设定好的策略对搜索结果进行打分，综合评估文本
的相关性与权限匹配度。

最后，我们会根据 topk 参数，挑选出排名靠前的若干个文档，并从数据库中进一步获取这些文档的元数据信息，比如标
题、作者、上传时间等。系统会对这些内容进行格式化处理，打包成清呆完整的响应结果，并最终返回给用户。

一、依赖的数据结构
01、MySQL表结构

document vectors 表:
字卫名           闫型                        拉还
< vedord         BlGINTAUTO_NCREMENT 。 主键，自增ID
ie md5       CHARG3)              文件指纹，用于关联fie_upload表
Cankid         INT                         文本分块序号
tet content 。 LONGTBG                   原冯文本内容
modal version 。 VARCHARG2)                生成向量所使用的模型版本
建表语句:

亨建file wpload厅

RearE TABLE "file upload 《
file md5”chAR(32) PRIWRY KEY COwWENT ,文人指纹
-flle_name”VARCHAR(255) NOT NULL comENT “文件各
total_stze” BIGIWNT USIGNED NOT NULL CoWEhT ,文件大小《字闻前》
tatug”TDIWINT(I) NOT ULL CoEMT 文件状态，
-wser_id ARCHAR(6A)》 NOT NULL COWEMT “再户生识，
 created_at” DATETINE pEFAULT CURRENL_TINESTAWP coEWT ,文件创建时间…，
merged_at”DATETIAE CORENT “文件并这由时间

Ceer ai

DB idcstatus， Cstatus )
TOBC idcreared_ar (Cereated_ar )

) ENGINE-Imna08 DEFAULT CHMRSET-utfanb4 COLLATE-utfasbd_ontcode_ct CORENT- "文件上传信息

<
字段名          类型                                                 描述
fle md5        CHARG2) PRIMARY KEY                           文件指纹
介e_name 。。 VARCHAR[255)                                     文件名
total_size     BIGINT UNSIGNED                        文件大小 〈字节数)
status           TINYINTCD                                          文件状态
senid wwRCHARG            用户标训

aeated t。 DATETIME DEFAULT CURRENT TIMESTAMP 。 文件创建时间

merged at 。 DATETME                                  文件合并完成时间

亨建eocument_vectors
cvEaTE Ta8tE document_vecrors 《
vector_id- BIGHIT NOT MULL MuTO_HcREhENT PRINpY KEY comeMr ,主妇，自大DD，，
ile mg5”chaR(32)》 NOT NULL COWEMT ,文件指纹，用于关联全le_upload表，
-enmk_1d-” INT NOT UL COMENT 文本分类序号
text_content” LONGTBIT NOT NULL ComENT ,原交人本内容，，
cdel_version” VANCNaR(32) NOT NULL CORRENT “生成向量所使用的模版本…，
le5 (la pa5
DB ideodel_veraion (aodel_veraion ]，
ssTRAINT “人epcument_vectors_file ”FOREIGN KEY (人ile sd5-) REFERENCES -但e upload- (file na-) on DELETE CascAoE oh upoy
) ENGTNE-Tmno08 DEFAULT CHARSET-utfBnb4 COLLATE-utfBsbd_onicoda_ci COHWENT-*文竺亲量存休表

02、Elasticsearch索引结构

文件共一标识fle ma5

分二序号chunicldl

文件文本向量存储

文本内导tod_content




## 第 3 页


、接口设计
01、 混合搜索接口

URL /apiyaearciybrid
。 Method: GET
parameters'

。 query 搜索查询字符串 《总需)
。 topk 返回结果数量 (可选，默认10)

示例; /apisearchyhybrid?query=沉点王二是沙崔topk=10

Response

{
file md5-: -abcla3。
eta

“而王二确实是沙对，你多的法模。-，

02、文档删除接口

URL /apiydocuments/ffile ads]
Method: DELETE

path Parameters
。 file_nds: 要王除的文件唯一标识 (MD5值)

Headers
etherizatior Bearer ftoken| 《用于身份验证)

-文折不丰在-

message"。-滩有要了到陈此文档~

messaga"。 -出于文档闪赂:详细鲁误信息-

三、派聪明发展阶段
第一阶段: Elasticsearch支持全文检索和向量检索

在当前阶段，派聪明系统基于 Elasticsearch 的强大功能，成功集成了两种主要的检索能力: 全文检索 和 向量检索，并实现
了它们的高效融合。

首先，在全文检索方面，我们借助 lasticsearch 强大的文本索引能力，配合 IK 分词器，实现了对中文内容的高效支持。系
统会对文档中的 text content 字段进行索引和匹配，用户输入的关键词可以被准确地分词并检索到相关内容，实现了类百
度式的文本接索体验。

其次，在语义检索方面，我们利用了 Elasticsearch 7x 及以上版本对 dense_vector 类型的支持。通过脚本打分机制，系统可
以根据用户查询向量与文档中 vector 字段之间的相似度进行排序，从而实现更"懂你-的语义匹配能力。这种方式特别适合




## 第 4 页


处理用户提出的自然语言查询，比如问题或句子。

更进一步的是，系统还实现了混合检索，也就是说: 在一次查询请求中，虐能进行关键词匹配 【全文检索) ，又能做语义相
亿度计算【向量检索)。通过设置内部权重，我们可以灵活地调节两种检索方式对最终结果的影响，实现更加精准和个性化
的排序。同时，由于是单次请求发往 Elasticsearch，不仅减少了网络延迟，也降低了整体的系统复杂度。

这样的架构设计带来了不少优势:

。 使用 Elasticsearch 作为统一的底层引擎，架构非常简单;
。 运维成本低，不需要额外引入复杂的向量数据库;
。 混合检索实现路径清晰，开发选代效率高

不过我们也清楚地认识到这套方案的局限性:

。 向量检索的性能拓纪明显，难以支撑大规模向量数据的快速检索;
。 尤其在并发访问量大时，内存和计算资源消耗较高;
。 随着数据规模的扩大，整体的检索响应时间也会变长，用户体验可能受到影响

因此，这套基于 Elasticsearch 的混合检索方案，适合用在中小规模场景，或者对检索实时性要求不是特别高的业务。在后
续系统演进中，我们也会评估是否引入更专业的向量检索引擎 (如 Faiss) 以进一步提升性能。

第二阶段: 集成FAISS提升向量检索性能

为进一步提升系统在大规模语义检索场景下的性能和扩展能力，派聪明系统计划在未来阶段引入专用的向量检索引车 一
FAISS。该优化将以"职责分离 + 双引擎协同-的方式进行，目的是在保持当前功能不变的前提下，实现更专业、更高性能的
向量计算能力。

在新架构中，我们将对检索功能进行明确分工:

。 Elasticsearch 继续承担全文检索和文档元信息存储的职责，利用其成执的文本索引和过滤能力;
。 FAISS 专注处理高维向量相似度计算，发挥其在大规模向量数据处理中的优势，如快速 opK 检索、向量压编等

通过这样的分工，可以充分发挥两个引擎的专长，避免性能瓶颈。为保障系统可维护性与扩展性，未来的检索架构将做如下
优化:

。 双引擎协同处理: 系统将建立 Elasticsearch 与 FAISS 之间的数据同步机制，实现向量和文本数据的实时协同;
。 抽象检索接口设计: 通过统一的检索接口屏蔽底层实现，便于根据不同场景灵活切换检索引擎;
。 接口稳定性保障: 对外暴露的 AP| 接口保持不变，确保上层业务系统无需感知底层技术变更，降低改动风险。

引入 FAISS 之后，系统整体性能棕有显著提升，主要体现在以下几个方面:

。 检索过度更快:向量检索响应时间从原本的百毫秒级优化到毫秒级;
。 系统香叶能力增强: 支持更高并发访问，QPS 预计可提升 5~10 售;
。 资源利用更优: FAISS 支持高效的内存结构和向量压缩算法，可显著降低内存和 CPU 占用，提逢整体资源利用率.

为确保切换过程平滑、用户无感知，派聪明计划采用以下渐进式部署策略

。 阶段一: 数据双写，在保证 Elasticsearch 正常工作的基础上，同时将向量数据写入 FAISS，验证同步稳定性 (~~这一条其实也可
以写到简历上~~)。

。 阶段二: 灰度发布，逐步放量部分检索流量至 FAISS 路径，进行性能对比和业务验证，确保兼容性无问题.

。 阶段三: 全面切换，验证稳定后，正式将向量检索请求切换到 FAISS，完成最终答换。

一一        23人已点和          一

全93063 :昌v

本                                                                  os 大本

热门评论 外

旦w

| base swsameaaammzisxemmmam

@派聪明 应该是子标签自动包含所有父标签的权限吧
四Bai

]                                                                                     2026年206日 2037

您指出的过加矛盾完全正确。根据权限系统的第规设计原则，子标签权限应自动包含其所有父标签的访问权限(即向
下继承) ，而非原文撕术的"父标签包含子标签权限'




## 第 5 页


工术限继承方向
子标签(如 技术部-前端组"自动继承父标签(如-技术部”) 的权限。这意味着拥有子标签权限的用户，天然拥有
父标签的访问权，但反之不成立-
文错误修正:

参考资料中的描述 *父标签权限自动包含所有子标签文档的访问权限" 应改为-

子标签权限自动包含所有父标签文格的访问权限-.

。 拥有 研发部 权限的用户 一 可访问 研发部 及其父标签 公司 的文档;
， 拥有 公司 权限的用户 - 仅能访问 公司 级别文档，无法自动访问子标签 (如 研闻 ) 的文档-

补充说明:

 。 哑认标签(DEFAULT : 所有用户均可访问，不爱权限标签限制

。 权限校验流程: 系统会逐级向上"用户权限，直到匹配到父标签或 DEFAULT 标签为止-
总结:

权限设计应名入 -向下继承"原则《子标签包含父标签权限) ，确保权限层级清且符合实际业务场景。原文表述存在
到名错误，需按上术修正理解

出s8 用四到

10条评论

网 asoa5                                                                2026年93月1日2215

@浙聪明，MySQL中存了哪些数据? 原始文本，分块文本，文本向量，文本元数据等这些信息者存在哪里?MySQL 的作用是什

]                                          026年月1日 2216
在MySQL中主要存储两关数据:
工人le_upload衣:存储文档元数据
。 文件指绞(fle md5)、文件名、大小、状态
。 用户ID、创建时间、合并完成时间
.document vectors表: 存储文本内容和分块信息
， 原始文本(text conten
， 人所标Rchunk id)
， 文件指绞(关联fle_upload表)
。 模型版本信息

各类信息存储位置:

。 原始文本: document vectors表的text_content字息
。 分块文本: 通过chunk id标识，每个分块对应一条记录
。 文本向量: 存储在Elasticsearch中，非MySQL

。 文本元数据: fle upload表

MysQL人用:

。 存储文档元数据和文本内容

。 提供结构化雪据管理和查询能力

。 通过外键关联文档上传信息和文本分块

。 支持基于用户、状态等条件的权限控制查询

而si 国占

加 ,58                                                                  2026年9月07日1216

引用原文: 建表语句

表名叫做document vectors 【文档向量) ，但字段中存储的是 tedt_content (文本内容] ，似乎还缺少了最重要的向量数据本
身?

而占叶 国卓81

他 oa                                                     2026和03月07日1217
@涛联明 回答一下
训兴”国回和

人的                                                                            26年03有06昌113




## 第 6 页


|

@源明 想同一下这个权重是怎么给出来的，是租路的给出来的吗
国昌si

]                                                                                     026征03有06昌115

根据参考资料，-通过设置内部权重* 吓指在混合检索第咯中，对语义检索(向量匹配) 和关键词检索 (全文区配) 的
结果进行加权咒合，以优化最终排麻。具体实现方式如下:

权重设定去辑
工本配置参数
权重值并非和路给出，而是作为可调参数由系统配置(如通过配置文件或管理后台设置)。例如-
， 语义检索权重:0.7 (影响向量相似度得分)
， 关键词检索权重: 0.3 《(彩响关键启开配得分)
(实际数值需要据业务场调优)
2 动态调整机制

系统可能支持动态权重，例如:
， 若用户查询全明确关键词 如"沉对王二是沙难") ，提高关键词权重;
， 若查询为语义问题 如 推荐一本搞笑小说") ，提高语义权重

3 打分公式示例
最终得分计算公束可能为:

复抽人

其中 vi ， va - 1，权重通过实验 (如A8测试 确定最优值_

是否-相咯-?

 非了隆设定-
权重需结合数据规模、查询类型、用户反馈等综合调优，目标是平街语义理解与关键局精准度，进免单一检索广
式的局限性。

优化方向:
参考资料提到第二阶段桂引入 FAISS 优化向量检索性能，届时权重第路可能进一步升级 《如引入机器学习模型动
坊优化权醒)。

权醒是可配置且入精细化调优的参数，通过陨合语义与关键订检索的优势，提升结果相关性。实际应用中需持续法代
优化，而非静态相咯设定-

让 用日

二 wanam                                                                2026年03月05日 2023

@浙玫明 文档进行向量化然后进行检索这里，我看到设置了分块字段chunlk id吗，这个字段作用是什么，是在哪个过程体现
了

出贞轩 国日81

1]                                          026征03朋05日 2024
在文档向量化与检索流程中，"” -snk_:a 字段的核心作用是标识文档的分块顺序"*，具体体现在以下两个关键过
娃:
1文档预处理阶段【向量化前)
分块处理:

用户上传的长文档(如PDF、Word) 会被系统自动拆分成多个文本块 例如每块512个字符)。
。 chankc_id 为每个分块分配唯一序号 【从0或1开始递增) ，确保分块顺序可所洲._

。* 示例: 一个10页的文档可能被折分为20个分块， chunk_ia 依次为0.1 2 -19.

数据存储关联
在 dccumenr_vecrors 表中，每个分块存储为独立记录，通过15:ie aas，<hunk_ial 组合键关联:

2 了个委红 (从a开内计共》
的有人文本上耻，

ea samer。 "及朱-pae
)

复审人权

。 用户可通过 -hunk_ia 知道该片段在文档中的位置如第3块) ，便于上下文理解




## 第 7 页


权限控制与上下文关联:
权限验证时，系统和”用户是否有权限访问该 :1s_ns5 对应的整个文档。
检索结果按相关性持序后， -hunk_ia 帮有动用户区分同一文档的不同片段，避免重复显示。

8结
nd 的核心价值:
解决长文档的分块处理与顺序标识问题，确保向量化时语义完整，检索时能精准定位原文片段-

， 体现过各
在文档预处理阶段生成，在检索结果中作为关键字段返回，吓连接向量检索与原始文档的桥梁-

出s间 国卓

二 Fnus                                                         assoanota za

引用原文: 在一次查询请求中，皮能进行关键河匹配 《全文检索) ，又能做语义相似度计算《向量检索)

用户输入问题 - 系统生成关键洒和内量-
构建 DSL ~ 同时包人关键河匹配和向量相似度计算.

区执行查询 -对每个候选文档计莽两个分数-

融合打分 - 按权重排序，取前topk

返回结果 -补充 MySQL 元信息，交给 RAG 模型生成答案
1 国昌和

国 xspeer                                                               2026和0有0日 2120

“引入 MCP 协议对本地文件操作、PDF 生成及数据库查询等能力进行 Server 灵封装，实现了Agent 与工具生态的解碳;
参考 OpenManus 开源架构，设计了分层智能体体系 (BaseAgent、ReActAgent、ToolCalAgent) : 利用 SpringAl

的 FunctionCalback 机制实现本地方法的自主调用，以及 ReAct 推理，晤予 Agent 思考-规划-行动-观察 的自主决

第能力，使其能够处理'检索/搜索-生成文本-生成总结PDF-等下多步骏的复杂任务;

“编写用户认证模块单元测试，结合 JUnit + Mockito 验证 Redis 引入前后的性能差异，为系统调优提供依据;

关于这几点，泥有明是怎么处理的?
@湖陪明
出点半”国旧E

]                                          026年01B 2121
根据提供的参考资料，派聪明项目在以下三个技术点的处理方式如下:

1 MCP协议封装工具能力

实现方式:
系统通过权限控制机块和AP接口设计实现工具与Ageni的解息。

， 本地文件操作、PDF生成等能力适过RESTful接口(如文格除接口 ap; /aocumonca/12ils_nasl ) 封半，
结合JWT认证 〔 xsthecszasicn 头) 确保安全调用。

，殖据库查询依者MySQL元数据存储如 :: 1。 .=1-*< 表) ，通过Elasticsearch温合检索提供统一数据访问入
D。
解抽带辑:
工具能力以服务化接口虹露，Agent通过调用标准化AP| (如 /api ssarhyhybz:a ) 能发操作，无需直接撞作底
展资源，符合MCP协议的Server吉封装原则

2 分层智能体架构 (BaseAgenWReActAgent/ToolCallAgent
， BaseAgent: 提供基础能力 (如权限验证、参数校验)。
，RehctAgent: 实现"思考-规划-行动-观察 流程:
。 规划: 解析用户查询 (如-检索-生成总结PDF ) ，分解为子任务 (调用混合检索接口
。 行动: 通过SpringAI的FunctionCallback调用工具《如豆包AP生成向量、Elasticsearch检索)
， 观察: 解析返回结果(如文档元数据、文本内容) ，动态调整下一步行动-

， ToolcallAgent: 封装工具调用过辑〔如权限控制、结果排序
， 复杂任务处理-
以检索-生成PDF为例:
1 RehctAgent蜗划任务 ~ 调用 /asi1eeacch/ayecia 获取文档-
了结合 docunene_vecrczs 表内容生成总结 ~ 调用PDF生成工具.
3 权限控制确保用户仅访问有权限的文档《基于标签过波)

.用户认证模块性能测试
测试方案
， 技术模: JUnit + Mockito，对比Redis组存引入前后的性能差导。
， 测试场景:
。 横扫训频权限校验请求(如基于"标签的权限验证).
， 对比直接查询MySQL 与缓存【Redis) 的响应时间-

， 调优依据-
， 组存命中:验证Redis对热点权限数据的加速效果 (如DEFAULT标签的全局访问权限) 。
* 资源消耗: 分析CPU/ 内存占用，优化缓存第略 如组存过期时间、Key设计)。




## 第 8 页


* 结果应用: 根据测试数据调整级存配置，提逢高并发场景下的权限校验效率-

总结
派册明通过接口化工具土著 MCP)、分层Agent架构 (ReAct+工具调用) 和数据虹动测试(盘存性能对比) ，实现了
工具解橘、复杂任务自主处理及系统性能优化，核依才现有技术栈 【ElasticsearcyRedis/MySQL) 和权限控制机

扎
南s8 国旧

全                                                          opsgozg24B 14
引用原文: fle upload

这个表作用是什么? 什么时候生成?
出并 用四

二                                                                  oz6soza2B ic

引用原文: document vectors

这个表作用是什么? 什么时候生成?
出上 国旧

全                                                                                             oz6sEo2朋24昌1613

引用原文: document vectors

这个表作用是什么? 什么时候生成?
黄 国旧和

是                                                ZoocwozpoeB xm
大福: 父村和要自动人所有于标签广的访问权隐

@源税明 应该是子标签自动包含所有父标签的权限吧
页点时 胃加1

人                                                                                     2026年2有06昌 2037

您指出的加矛盾完全正确。根据权限系统的第规设计原则，子标签权限应自动包含其所有父标签的访问权限《即向
下继承) ，而非原文拉术的"父标签包含子标答权|

工术限继承方向-
子标签 (如-技术部-前庙组') 自动继承父标签 (如-技术部-) 的权限。这意味着拥有子标签权限的用户，天然拥有
父标签的访问权，但反之不成立。

2 原文错误修正:
参考资料中的描述 *父标答权限自动包含所有子标签文档的访问权限 应改为:
子标签权限自动包含所有父标签文档的访问权限

3 实际场景示例-
， 父标签: 公司

， 子标签 研几钴 、 市场部
权限规
， 拥有 研发部 权限的用户 -可访问 研发部 及其父标签 公司 的文档;
， 拥有 公司 权限的用户 ~ 仅能访问 公司 级别文档，无法自动访问子标签 (如 研几部 ) 的文档。

补充说明:

。 标签(DEFAULT : 所有用户均可访问，不受权限标委制-

。 权限校验流程: 系统会带级向上"用户权限，直到匹配到父标签或DEFAULT 标签为止。

总结:

权限设计应遵避 “向下冯承"原则《子标答包生父标签权限) ，确保权限导级清跌且符合实际业务场景。原文表述存在
到组错误，天按上术修正理解

国




