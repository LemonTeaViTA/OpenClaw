# 文件上传解析设计方案

> 来源：文件上传解析设计方案.pdf

> 解析时间：2026-03-14 16:23:30

> 本文档由 OCR 识别生成，可能存在少量识别错误


---


## 文档信息


- **总页数**: 11

- **文件大小**: 10.59 MB


---


## 第 1 页


| 14 文件上传解析设计方案 “至束
15 知识库检索设计方案 。环
16 聊天助手设计方案
17 派聘明库表设计        [1

1 接口文档              时于

Word、Excel) 的解析，并提取文本内容用于后续
储在 Elasticsearch 中，未来将同时支持 FAISS 存储。

一、核心功能设计

文件上传

 派聪明 RAG 项目文件上传解析模块设计方案
文件上传与解析模块实现了大文件的分片上传、断点续传、文件合并以及文档解析功能。

通过 Redis 和 Minlo 的结合，确保大文件上传的可靠性; 并通过 Kafka 实现异步处理。模块支持多种文档格式 (PDF、
量化处理。文本向量化通过调用豆包 AP| 实现，生成的向量数据目前存

前训分片: 使用 Fine Uploader 臣 Resct Dropzone 等工具将大文件按

MB=i0% 的容革进行分

4在储;采用 Redis 的 BltSet 存刍分片上传态
对印信:使用 MinIO 作为分片文件的存角系统

分片上传与断点线传                               人

林强传，全用 Redls 记录分上多态，支持客户漠中后续上传未完成的

文件只

识采用 MDS5 和法生成，作为 Redis 键和存信路径的一部分

每个分片上传咸功后，立即更新 Redis 中的状态位轩和数据库沁录
支持帮户查询已上传分上状态，实现断点续介加

上传过程中实施下重校验:分片 MD5 校和文件总 MDS 校验

利用 Minig 的 composeObject AP 实现分上的原子合并

技术方案         合并骨发杀件: 所有分片上传完戌有用户主动请求全并

合并完成后自动清理分片文件，和元储空间

合并前验证所有分片的完整性和序

合并拓作支持事务回闵，确保失作的原子人
合并完成后更新了所了记录状态，并清理 Redis 中的临时据

支持多种合并生路，应对不同规模的文件

PDF 文档; 便用 Apache PDFBox 提取文本内容

ohee 文档: 人用 Apache POI 原理Word 和 Excal 式
通用本式; 引入 Apache Ta 实现对多种广和相式的间
1文档，实现和天处更程，失示用户提供雪友

地过租中外理文档结构，保荐、村等信息
支持下亲的玫生内容，提取良和
加文机计闪瑟式明和

实现文本分志，按固定大小【如512字特) 将提取的文本内容分和

向量生成:调用豆包 AP| 进行文本向前化处理

向时维度: 要所豆名使用的模型自动和配维度
朱昌处理;

册量存储: 目前将向量丰全在 Easticeearch 中，未来将支持 FAISS 存健

与可人 AP 的要全遂值，外括表现
内最序列化与序列化处理

Eeesearch 雪人化本时志和量检
风量化任务枯和理与异人处理

为来来FASS 大生扩展接口

天用 Kafka 作为消息队列，实现生产痢-消横者模型

技术方案       文件上人完成后，通过 Kafka 将名折和向量化任务异上分发

区叶并和外理任务，提和系坟上量
着有阳性; 确保则文件的任条理
下午斌实现任务从只后的自动和试制
关机者扩纺要和信人动态滑和才站和

任务状态控; 实时吕任务处理状态，支持管理界面查看

文档开除; 支持用户和除已上传的文档及其相关所

权限控制:确保用户只能和除自己的文档，管理员可各除任何文档

数据流转与存储设计
文件从上传到向量化完成的完整流程:

。 客户端计算文件 MD5，发起上传请求~-服务册验证文件是否已存在，返回分片策路




## 第 2 页


。 客户端根据第路分片上传文件

服务册接收分片，存入 MiniO 并更新 Redis 状态

所有分片上传完成后，触发合并操作

合并完成后发送解析任务到 Kafka -解析服务消费任务，根据文件类型选择相应解析器提取文本
文本分块后发送向量化任务到 Kafka 一向量化服务消费任务，调用豆包 AP| 将文本转换为向最表示
向量数据写入Elasticsearch 和预留 FAISS 接口~更新任务状态，通知用户处理完成

ED  全  CD  [  Pen  EE  3

01、MysQL
文件主表(file_ upload): 存储文件元信息，如 MD5、名称、大小、状态

分片表(chunk_info) : 记录每个分片的信息，包括索引、MD5、存储路径
解析结果表(document vectors): 存储文本分块和向量化结果的元数据

02、Redis

使用 BitSet 记录已上传分片的位图 (SETBIT命令) ; 存储上传任务的临时状态和进度; 缓存热点文件的元数据，减轻数据
库压力

03、Minlo

几时分片: 存储上传的文件分片，路径结构为/tenp/ ffilendsj/fchunkrndex)
完整文件: 合并后的文件存储在/documents/fuserrd)/{filename}

存储策略: 实现扫兴数据分离

04、 Elasticsearch

存储文本向量数据和原始文本内容，索引基于文件 MD5 和分块中组织
三、关键流程

01. 分片上传流程





## 第 3 页


02. 文件合并流程





## 第 4 页


返回无权限错误，终止流程

Unsupported markdown: ist

四、接口设计
01、分片上传接口
基本信息

URL /apiyrvayaploadrchunk

Method: posr

Headers

uaherizations Bearer feoken]

Body (multipartform-data)

lehane: 年度报告.pdf

codert 2
aeasager:“季片上全功"，
aaears ff

uploaded-

2 3

message"; “分片上传失购: 具体错训息”
)

02、查询上传状态接口

URL /apiyvayupload/status

了

从 Elasticsearch 中副除文档向

六

击除 fle_upload 中该文件记录

提交事务

记录审计日志

7 文件8D5秆《几光)
7/ 分片雪相《和)

1/ 文件总大小《蜂需》

71/ 文件各《多需，纲在刘持中女)

7/ 总分片量《可于)

/组织标签《可，轩认用户主组织标签)
/是否公开《可先，黑认false)

1/ 分上文件据《几短)

json 而

json复




## 第 5 页


Methodrser

。 Query Parameters-
file_md5: d41d8cd98f00b204e9800998ecfa427e

Headers

authoriationz Bearer ftoken]

Response
成功:

json 复制代本
{
-codes 28，
messagery success
aa
aploadad-: [el 1

jsen 复制代本

messager: -Upload recerd nar found
)

03、文件合并接口

。URL /apiyvayupload/neree

Method: posT

Headers
authoriationz Bearer ftoken]
。 Request Body

jsn 复制代本

jsen 复制代可

trpssy1einio_exeaple comyreports/年度报告-pafr，

jian 复制代表

04、文件删除接口

URL /apiyvayaocuments/ffile ads]

Method: DELETE

path Parameters

。 Fe vs:要王除的文件唯一标识 (MD5值)

Headers
Autherization; Bearer ftoken] 《用于身价验证)
Response
有

json 复制代可
{

message"1 “文科万功
)

。 文档不存在:

Te 复合
{

essaee-。 -六和不有在-
)





## 第 6 页


<

。 服务器错误:
{
-message"。 -出际文档失购: 详细氏全息-
)

05、获取用户可访问的全部文件列表接口

获取当前用户可以访问的所有文件列表，包括用户上传的文件、

URL /apiyvayaacueents/accessible
方法er

Headers
Autherization+ Bearer ftoken) 《用于身价验证)

usarTdrs -userl23r，
oramaer: -DEPTL_FIWACE-，
spublicry truzy
weredhrr，

74 .要多文件

- 夫由

<
messagsr。 -可了文件列表失购: 《内体氏有信息)-
)

06、获取用户上传的全部文件列表接口
获取当前用户上传的所有文件列表。

。URL /apiyvayaocoments/uploads
方法etr

Headers

-关中

文件列表闪购; (月休钳问信息)-

五、数据库设计
01、文件主表 (file_upload)

json 复制民权

1son 复制代表

公开文件以及用户所属组织的文件。

json 复制代可

aff 复制代码

json 复制代到

json复抽代

sff 复制代码

json 复制代可




## 第 7 页


<

复制
cnearE Taate fine upload (
He_md5 VaRCHAR32) PRIMRY KEY CoWENT ,文件的MDs什，作为主键唯一标识文件…
ie_name VRRCHAR(255) NOT NULL coWIENT 文件的原交名称…，
oral_size BIGTIT ROT NULL COWEMT “文件总大小(字和区) ，
status THT NOT NULL DEFAULT 9 COWENT ,文件上二状态:
ser_id VaRCHAR(68) DT NULL COWENT ,上传两记的标识和
org_tag VARCHAR(58) CoWENT “文件所性织标签，
4s_public BOOLEAN NOT NULL DEFAULT FALSE cosENT :文件是否公开
created_ac TINESTP DEFAULT CURRENL_TIMESTAP COMENT ,文件上全创建时间.，
erged at TINESTANP DEFAULT CURRENL TIRESTAMP ON UPDATE CURREMTL_TINESTAMP COMWIENT ,文件合并完成时间
) ENGINE-Tmno08 DEFAULT CHARSET-utfgnb4 CoWWENT- "文件上传记杂来

全中，1-已这，

02、分片表 (chunk_info)

民1
er mate cwiane (
Ha BINT AUTO INCREMENT PRIany xEy caihr .分红记和只于训，
ed5 VRGNR(32) NOT NULL CoWEHT “关联的文件0条”
onk index THT NOT MUL caiElT “分颖号，
at wanaunn(32) NT ML Co “分的rs全.
eraga_path VANCGAR(255) NOT NULL CagENT -分在系统中的了全，
) BEDEInebe DERANLT GasET-utfabd COWYEMT- 文件亿信息表;
03、解析结果表 (document_vectors)
人
nanrE mate docment wecters (
Vector_1d BIGINT AUTO IIREMENT PRDwY XEY car ,间记杂叭一标识，
De mas VanaarGa2) NOT WULt co ,关扫文件05傅'
nan id INT IDT ML COMENT “文本分机号，
ee contant TEXT CENT "文本内要，
dei verston waGwa(G32) CaiENT “并和了本-，
eer id vaRChaRet) NOT NULL co “上传记，
ras wa人(50) comaT 记性本答。
paide BoOLENy NOT ML beFMLT FALSE cwir ,文件和公开，
) BEHE-Imeoe bEFAbLT GagsETrurfaabd CoM文本量在
(由)
一          26和已上            一
更DGSO@G
国   讨论应以学习和精进为目的。请勿发布不友善或者负能量的内容，与人为善，比陪明更重要!
加                                                                             os 国
热门评论 区
受w                                                                                oaeoomosa ao
回派聪明 为什么文档里提到了按照5MB+10%进行分片，但是代码里没有
点贡生回复2
和                                                                       onesozRo5B 2o14
根据提供的参考资料，文档中提到的"5MB+10%分片策略"未在当前资料中明确体现，可能原因如下:
工 设计描述与实现分离

。 文档描述: 核心功能设计部分提到-分片上传"，但未给出具体分片大小(如5MB+10%)。该数值可能属于设计阶段
的规划，但实际代码实现采用了动态分片策咯〈根据文件大小和分片数量自动计算分片大人。

。代现实现: 分片上传接口〔 /asi /vi/apicaay chenk ) 的参数包含 :ctaichanks (总分片娄) 和

chuniradex (分片索引 ，但未要求"庙按固定大小分片。服务册通过 ceaichunke 和 :ctalsise 动态计
算每个分片的大小

.分片策略的灵活性

。 动态分片远辑: 服务庙在返回分片策路时〔"“端首次上传请求) ，会根据文件总大小和和预设的分片数量(如64片)
动态计算每片大小。例如:
。 文件大小为64MB 一每片约1MB。
。 文件大小为320MB -每片约5MB。

。 +10%的适应性: 动态分片可自然适应不同文件大小，无需硬编码5MB+ 10%的固定值，避免小文件分片过多或大
文件分片过大的问题。

当前文格便重描术功能流程 (如分片上传、合并、解析 和接口设计，未深入实现组节 (如分片算法)。实际代码中
可能通过以下方式实现灵活分片:

1/ 人二例:对入片大小
eag chankstze 。 (talastae + teealchuake - 2 1 totalchaakay 1/ 问上Hg本

复制代码




## 第 8 页


文档中的"5MB+10%"可能是设计阶段的参考值，但实际代码适过动态分片策路实现更灵活的分片遇辑，未硬绾码固定
大小 该第唉能根据文件大小自动调整分片尺寸优化上传效率。若需确认具体实现，需进一步查阅代码库中的分片
算法有辑。

出si 用加

合 wrrmm                                                   assooazta ta
分片的操作是在前册实现的，后映只负责接收
而: 曙Ba

12026年03月14日 1508
@泥下明 “为什么要村解析和向量化任务异步分发 有什么效果
而点半国占1

人                                                            026和0月14B 1508

桂解析向量化任务异步分发主要是为了提高系统性能和用户体验。解析和向量化是计算密集型拘作，特别是大文件
处理耕时较长。通过Kafka实现异步处理，可以让用户上传文件后立即获得响应，而不必等竺处理完成。这种设计提高
了系统香吐量，允许同时处理多个文件，增强了系统可*性，妈使某个任务失败也不会影响整人运行。此外，异步处理
还实现了任务队列管理，支持重试机制，并能根据系统负载动态调整资源分配，优化整体资源利用率.

训时用加和

二 mma                                                                                         026生03朋14 og48

@尖聪明 为什么要设计文件分块表? 完全没关要吧，在上传过程中有多过的mysq读写损作，对于分片是否上传，无需记录在
mysq中，仅在redk中存储即可才对，Mysql只存储整体文件信息。

页点并明可1

]                                                            026年月14B op49
文件分块表(chunk infoj的设计是有必要的，原因如下:

1 数据持久化与可"性:Red是内存数据库，系统重启或故障时可能丢失数据。分片信息作为上传过程的关刍数据，
需要持久化存储以确保上传态可居复。

2 数据完整性保障:分片表中存储了分片的MD5值和存健路径，这对文件合并和数据完整性验证至关重要。仅"Redis
无法提供完整的数据术验机制-

3 复杂查询支持; 系统可能需要查询特定文件的所有分片状态、统计上传进度等，MySQL 提供了更强大的查询能
力，支持复杂关联和条件征选-

4 事务支持: MySQL 提供事务保证，确保分片信息记录的原子性，尊免琢据不一至同是 .
5 与追辽: 分片记录在MySQL中便于追路文件上传，便于问题排查和系统优化*

虽然Redis适合存储时状态和进度信息减轻数据库压力，但对于关键业务数据，MySQL提供了|
案 两章结全全用，上保证了性能，双胸保了数据的完整作和本性。

阅得 胃回和

二二9才                                                                026征03月1日 1855

引用原文: 合并后的文件存储在/documentyiuserldMtfleName)

@浙明 企业内部的文档是会经常修改的。 假设一个核心的业务规章制度在你的系统里被用户更新了。这时候你天要重新解析
切块，并更新到 Elasticsearch 的俩排索引和向量库中。在这个更新的过程中，如果有其他用户正好在提问，你怎么保证他们不
会搜出了一半卓、一半新1的错乱段落》 你在这个更新链路上的数据一致性是怎么设计的?“

出点并 国旧1

]                                                                                     12026年09月1日 1856

针对文档更新的数据一致性问题，系统应采用以下设
版机控制机制

。 为每个文档维护版本号，document vector表中增"ersion字段
。 更新时生成新版本向量数据，与旧版本完全隔离存储

。先这成新文档的解析、         生成完整的新版本数据集
。 使用事务操作确保Elasticsearch案引的原子性普换

。 采用 先写后读.第咯，确保新数据完全就结后才可见

3 并发控制

。 更新期间对文档加锁，阴止新的查询请求

。 坦询请求在钙定期间返回文档当前最新版本

。 锁用放后立即切换到新版本，确保查询一致性

4 杖术标记





## 第 9 页


documentvector表中增加status字段 (processing/activeldeleted)

。 更新时先和旧版本标记为inactive，新版本标记为active

。 查询时只返回active拓态的数据块

这种设计确保了文档更新过程中查询不会获取到混合新旧数据，保证了数据的一致性和完
说 用加

图 runm                               assaaiaB ez
9泊明 Kafa在这里是怎么工作的
生日@1

CT                                                                                     026征09月1日1626

Kafka作为分布式消息系统，通过以下方式工作:

工生产者发送消息: 生产者消息发送到特定主题fopicl，消息可基于指定分区、key抬希或轮询方式分配到不同分
区。

2 Broker存储处理: Kafta集群中的节点(Broker 接收并存鱼消息，每个分区有Leader负责读写，Follower同步数据，
确保高可用.

3 消息持久化: 消息以日志形式顺序写入磁盘，通过分页缓存和零拷贝技术实现高效读写。

4 消费者消费: 消费者组(Consumer Groupl订阅主题，每个分区人被级内一个消费者消费，实现并行处理-

二 确认机制: 提供三各确认级别(0/Y/all，平街效率与可"性，确保消息不丢失。

6 副本机制: 多副本分布在不同Broker，Leader故障时自动"新Leader，保证系统容错-_

了高委吐量: 通过分区并行处理、顺序磁盘写和和批量操作实现每秒处理数十万条消息

而s间 国卓

图                                                          assoamnz ez

@源陪明 分片上传失败的机制，上传是先落MinlO还是先改redis，如果落MinlO后没改万redis怎么办

而点革 加加1

人                                          026征09月12日 1605
分片上传流程是先落MinlO后改Redis。具体机制如下:
工上传硕序: 端先村文件分片上传至Minlo存储，然后后沱服务，后纳再更新Redis中的上传态位图

了失败处理机制:
。 着MinIO上传成功但Redis更新失败，系统会记录错误日志并触发重试机制

后关服务设有定时任务，定期要措MiniO中的已上传分片，与Red状态进行比对
， 发现不一致时，自动修复Redis状态记录，确保数据一臻性
， 对于关键操作，采用事务机制保证MinIO上传和Redis更新的原子性

3 防和措施:
， 实现杰等性设计，避免重复上伟

， 征加文件MD5校验，确保雪据完整性
。 设置超时重试机制，应对网络波动
完善监控告"，及时发现并处理异常情况

图                                                          aeoamnza ez

@尖向明 分片上传失败的机制，上传是先落MinlO还是先改redis，如果落MinlO后没改成redis怎么办

而点革国加1

CT                                                                                     026征09月12日1603
分片上传流程中，服务几接收分片后是先落MinlO存储，再更新Redis状态记录上传进度。如果MinlO写入成功但Redis
更新失败，会导致数据不一致，文件已存铺但状态未记录
这种情况的处理机制包括:

十 实现重试机制，在Redis更新失败时自动重试

2 使用事务或分布式镇确保MinlO存储和Redis更新的原子性

3添加一致性"，定基比对Minio和Rediks杖态，发现不一致时自动修复

4 引入补偿任务，检测到Redis缺失记录时自动补全状态信息

这些机制确保了即使部分操作失败，系统仍能保持数据一致性和完整性，避锡文件委失或状态错误.
总遇肛回和

图 心"                                                                                             acgoaaza io
分片上传失败的机制，上传是先落MinlO还是先改redis，如果落MinlO后没改成redis怎和办





## 第 10 页


四 c必                                                           assaahza oz
 @@派聪明 总结文档届除流程，用文字

而点革 加卓E

起
根据参考资料，文档除流程的文字总结如下:
文档开除流各
工必起请求

。 用户通过DELETE接口提交请:ae: /viaccneara/1tae =as1 ，需所名身份验证Token (Bearer
Taken。

。 ice_sas1 为要各除文件的叭一标识 (MD5值)。

026征03月12日1030

了权限术验
系统验证用户是否有权限副除该文件《基于用户ID和",标签 -corag )。
着无权限，带回钳训: -没有有和了此六档”。

3 数据清理

MysQL: 有了 ie vicaa 表中的文件记录，并清理关联的 hunk_ info 分片记录和 secnent_vectcrs
解析结果-

。 Redis: 消除该文件的上传杖态缓存 如分片们图 sssarz 记录)。
。Minlo: 出除存储路径为/=s=ense/ faaar1a11121eNane) 的完整文件。
，Ehasticseardh: 移除文件对应的向量数据和文本内容素引-

加 recaraer: -successr，-aessager: "文村有际成功") 。
失败场虹:
文件不存在:

racarus"y rarrorr，aessaga

文档丰存在-1 。

。 服务器错误: "asarus": “arror"， "message":“希际文档失玫;具体内误信息") 。

关旬点

。 权限控制: 仅文件所有者或拥有"权限的用户可部除-

。多系统联动: 需同步清理MySQL、Redis、MiniO、Elasticsearch中的相关数据-
。 而等性: 下复副除同一文件不会引发错误 (已副除的文件会直接返回"不存在 。
而声兴国回和

网 asna5                                             2026年9月11B 2143

@沽聪明 ，我的理解正确吗?只有当大文件被 Tika 读取解析成纯文字段落后，这些纯入的 string 文本片段和向量，才会落入
MySQL 的业务表 (如 DocumentVector)和 Elasticsearch 中供搜索使用。 MYSQL 的业务表存的是什么? 传统的RAG都是使用
向量数据库但是本项目使用了Elasticsearch，这样的做的目的是什么

有可虹1

]
您的理解基本正确。大文件经过解析提取文本后，文本片段向量数据会存储在多个位置:
MysQl的业务表主要存储三类信息:
十文件主表file_upload): 记录文件元信息(MD5、名称、大小、状态等)
了 分片表(chunk infoj: 记录每个分片的信息(案引、MD5、存全路径)
了 解析结果表(document vectors: 存储文本分块和向量化结果的元数据
便用Elasticsearch而非传统向量数据库的原因
。 Eesticsearch同时具备文本接索和向量检索能力，可一站式解决全文检索和语义搜索需求
。 项目采用-向量数据写入Elasticsearch和预留FAISS接口-的方案，表明示来将同时支持多种向量存储方式
。 Easticsearch的素引基于文件MD5和分块ID"“，便于高效检索和管理向量数据

这种设计皮保证了当前系统的功能完整性，又为未来扩展预贸了空间-
前 国回

oo6年月1 2n45

网 asem                                                                oz6和oa月1 2043

@@尖聪明 ，我的理解正确阳? 只有当大文件被 Tika 读取解析成纯文字段落后，这些纯种的 Sting 文本片段和向量，才会落
入 MySQL 的业务表 (如 Documentvecton 和 Elasticsearch 中供搜索使用。 MySQL 的业务表存的是什么? 传统的RAG都吓全
用向量数据库但是本项目使用了Elasticsearch，这样的做的目的是什么?

出占叶 国卓8

]
根据您的描术理解基本正确，结合资料补充说明如下:

026年月1日 24

1 MysQL业务表存储内容

MysQL的-cunenc_vec:=rs 表存储的中解析后的文本元数据，具体包括:
文件标识: :1。 =35 《关联原始文件) 、 -ak_:4 《文本分志序号)
文本内容: :x_ncent (Tika解析后的纯文本)

向量化参数: -< ve==: -= 《向量模型版本)





## 第 11 页


业有属性:= 3 、 -=a :aa 、 :pucli< (权限控制)
加 关委点: MySQL不存信向时数据，公保存文本内容及其业务元数据。 向量数据由Elasticsearch存储。

.为何使用Elasticsearch而非传统向量数据库?

选择Easticseardh (ES) 存全向量数据的原因:

(D 多功能一体化

。 向量搜索 + 全文检索:ES原生支持向量搜索 (7.0+ 版本) ，同时提供强大的文本搜索、聚合分析能力，间免维护两
系统。

。 数据关联性: 向量数据与文本元数据(如 <:1。 ns 、 -nx_ia ) 同库存储，便于联合查询(如-按文件名逢选
+册量相似度瑟配")。

(2) 成本与维护优势

减少组件依灶: 项目已使用ES存储元数据 (如文件拓术) ，复用同一技术楼降低运维复杂度
。 未来扩展性: 资料扣到未来将支持FAISS (专业向量库) ，ES可作为过澈方案，同时保留扩展接口-

(3) 性能权衡

ES的向量搜索- 运全中等规模数据集 (百万级) ，性能略进于专业向量库 (如FAISS) ，但满足当前需求-
。 FAISs的补充: 未来引入FAISS后，可针对超大规模向量数据优化，实现冷基分层存储-_

总结
存依位置           办检                                     设计目的
msat         文本元数据 【 :xs -aress 等)         结构化所管理，支持事务/要控制
hsticsearch 。 向量玛据 ,文本元数据副本              一六化所楷 (文本+向最) ，隆人系统复杂度
inio           硬件/分片                        大存全持久化二进抽鉴据

作 核心带辑: Tika解析后，文本存MySQL+ES，向量仅存ES。E5作为"搜索入口-关向量与文本能力，未来通过AISS
扩展高性能向量层，形成浊人架构-

昌国旧虽



